# Plugin template

The plugin template that `ncx plugin new` copies: a project that references `Ncx.Plugins` alone, one rewriter, one test and the six steps of this README (`../../docs/architecture/code-guidelines.md`, section 11); task P7-02 fills this folder.
