# Sample plugins

Small finished plugins that double as documentation, each with a test: the coolant clutch rule, a block writer that puts `Z` on its own line, and a source rule that folds `M5`, `M51`, `M3 S` back into `COOLANT:THROUGH=ON` (`../../docs/architecture/architecture.md`, section 3); task P7-02 fills this folder.
