# P4-02 Analytics: tool list, runtime estimate

Phase: 4 | Milestone: M7 | Depends on: `P4-01` | Size: M (S: a day or two, M: up to a week, L: more)

## Goal

The first two analytics as event subscribers in `Ncx.Analytics`.

## Scope

- Tool list: per TOOL_BEGIN/TOOL_END the tool, holder, speed range, cutting distance, rapid distance, block count, time.
- Runtime estimate: the trapezoidal profile of section 8 of the virtual machine with `max_feed`, `acceleration`, `[dynamics]` block time and `path_mode`, spindle `accel_time`, dwell, cycle sequences (with `ExpandCycles`); per tool and total; rapid at the axis maximum.
- `ncx analyze <file> [--machine <toml>] --from <line> --to <line>` with a text report (D67; `--machine` optional as for `check`, D103).

## References

- ncx-virtual-machine.md section 8
- architecture.md section 9 (analytics table)

## Done when

- The tool list of `2.5D_FRAESEN.ncx` names two tools with the right distances.
- The runtime of `3D_FRAESEN.fanuc.nc` (converted) is within 10 percent of the value the maintainer measured on the machine (ask for it); the estimate is documented as an estimate.

## Log

(who, when, what was decided while doing it; a decision that changes the specification gets a row in `../../../decisions/decisions.md`)

Claude (agent), 2026-09-14, P4-02 Analytics: tool list, runtime estimate. Built on `main` at ecf04e7: the virtual machine in both modes with its events (P1-05, P4-01), the machine records of D107 with `[[axis]]` rapid, max_feed, acceleration and `[dynamics]` (P2-01), the pipeline of the CLI (P1-07, P2-04, P4-01). Decisions implemented: D37 (analyze always runs with `ExpandCycles`), D64 (trapezoidal profile, deceleration equal to acceleration, block_time, path_mode with corner_speed, accel_time), D67 (every analytic takes `--from`/`--to`), D103 (analyze without `--machine` runs against the default machine). No decision of the log was needed; the specification is unchanged except the analyze row of architecture 10 (F28).

- Files: `src/Ncx.Analytics/`: `IAnalytic` (an `IVmListener` with `Range` and `Report()`), `BlockRange`, `AnalyticOptions` (machine, the names the report gives file and machine, range, format, mode), `AnalyticsRegistry`, `TextTable` with `TableFormat` (aligned columns or CSV), `ReportText`, `RangeFilter`; `Runtime/`: `RuntimeAnalytic` (with `.Report.cs`), `RuntimeEstimator` (the model as a stream: every event in, every time charged out as a `TimedStep`, one `PendingMotion` kept back for the corner speed), `MotionProfile`, `MovingAxes`, `CommandedFeed`, `SpindleSpeed`, `MachineDynamics`, `TimeTotal`; `ToolList/`: `ToolListAnalytic` (with `.Report.cs`), `ToolUse`. `Placeholder.cs` is gone. `src/Ncx.Cli/Commands/AnalyzeCommand.cs` and `AnalyzeSettings.cs`. Tests in `tests/Ncx.Acceptance/Analytics/` (`MotionProfileTests`, `RuntimeAnalyticTests`, `ToolListAnalyticTests`, `TextTableTests`, `BlockRangeTests`, `AnalyticsRegistryTests`, with `AnalyticRuns`, `AnalyticMachines`, `AnalyticsReference`) and `tests/Ncx.Acceptance/Cli/AnalyzeCommandTests.cs`; the references `tests/Ncx.Acceptance/Expected/analytics/2.5D_FRAESEN.tools.txt` and `3D_FRAESEN.fanuc-mill-30i.runtime.txt`.
- Runtime estimate, readings of VM 8 (not questions): the path speed is the commanded feed limited by the smallest max_feed of the moving linear axes, RAPID and HOME at the smallest rapid of them, accelerating with the smallest acceleration of them ("limited by the max_feed of every axis that moves", "the smallest acceleration of the moving axes"). A rotary axis adds no travel and sets no limit, because the runtime of rotary moves belongs to the kinematics module (VM 9); the report counts the motions that turned one. Under the polar and cylinder planes the C word is a length (VM 3.1, D102) and counts like the others. The block_time floor applies per MOTION (phase file), so to each motion of an expanded cycle. In continuous mode the corner speed between two motions is the smallest of their two speeds, corner_speed and the speed the first can reach by its end; the time of a motion is final when the next one arrives. A motion of unknown length, a motion without a known feed (a feed per revolution while the spindle stands among them) and a RAPID without a rapid rate are not timed and are counted in the report; the path comes to rest around them. The feed per revolution and CSS follow the spindle of the holder of the last TOOL, the default spindle for a holder without one, as VM 5 reads the spindle of the tool holder. The machine file gives mm/min and mm/s^2 (machine-config 4), so an INCH program is converted, and VC is ft/min under INCH (language 4.11). CYCLE_DWELL is charged once per call whose motions the virtual machine raised (VM 3.3 names one dwell at the depth); a call without motions (a catalog cycle, `CYCLE:<controller>=n`) is not timed and is counted; the spindle reversal of TAP adds nothing, as no key gives its time. Without `[dynamics]` (the phase file's trigger) the estimate is distance over the commanded feed, RAPID and HOME distance over the rapid rate, no max_feed, acceleration or block time, and the report says so. A `[dynamics]` without path_mode is exact stop, without corner_speed stops in every corner, without block_time has no floor; an axis without acceleration moves without acceleration; the report writes each missing value as `none`. Tool changes add no time (no machine key), which the report says.
- Totals: per tool by holder and tool, per SECTION by its text in the order it first got time (with the line of its first SECTION; the time before the first as its own row), per channel; the time of the run is the longest channel. The waits at the marks come with the job scheduler: a plain `TODO` for P6-01.
- Tool list, readings: the phase file reads "names two tools" of 2.5D_FRAESEN as tools 1 and the preloaded 0, which is no tool, so the list has one row. Cutting is LINE, ARC and the LINE motions of an expanded cycle, rapid every other motion (RAPID, HOME, RETRACT, the rapid motions of a cycle); distances in mm from the MOTION lengths; unknown counts the motions of unknown length. `preloaded` answers "was the next tool preloaded before the change" on the row of the tool that arrives, from the Before of its TOOL_BEGIN, which the PRELOAD words set. rpm and feeds are those of the cutting motions, offsets the registers of the holder that are not 0 during the use. The block count runs from the block that brought the tool in to the block before the one that took it out (VM 7, TOOL_END); a block without an event (a LABEL, a COMMENT) is counted in INTERPRETED mode through blocksExecuted (VM 2.7) with the block before it, and is not seen in STATIC mode, which does not count blocksExecuted. The time is the runtime estimate under the tool.
- Block range: the estimate runs over the whole run, so that the motions at the ends of the range enter and leave with their true speeds, and only what stands on the lines of the range is summed and counted; a block of an external program has the lines of its own file and is outside every range (VM 8: "NCX line numbers of the file").
- Reports: the title names the analytic, the file name, the range, the machine file name ("the built-in default machine" without one) and the mode; every report says estimate, and the runtime report lists every value of the machine file it reads (the linear axes and the spindles), so that the maintainer corrects the file (implementation 14, risks). `--format csv` writes the lines of text as rows of one field (the reading of wave-2 question #40).
- CLI: `ncx analyze <file> [--machine] [--vars] [--from <line>] [--to <line>] [--analytic tools,runtime] [--static] [--format text|csv] [--skip-blocks none|all|1,3] [--strict]`. `--skip-blocks` because D53 lets analytics report both; `--expand-cycles` is not offered, as analyze always expands. `--from` and `--to` are 1 or more, `--from` not after `--to`, either end open; `--vars` with `--static` is a usage error, as `--vars` without `--interpreted` is for trace; `--analytic` takes the names in the order written, all registered analytics in their order without it, and an unknown name is a usage error that lists them. Every mistake of the command line is CLI001 and every unreadable input keeps its code (CLI002, CLI100, CLI101, CLI200 to CLI202), so no code of CLI450 to CLI499 was needed; the analytics report in their text and raise no diagnostic, so Ncx.Analytics has no `DiagnosticCodes` class and ANA001 to ANA099 stay unused.
- Open questions, marked `TODO(question)` in the files: under CSS, the X radius of a motion that changes X is the one it starts at (`SpindleSpeed.CssRpm`); a spindle start to less than rpm_max adds the whole accel_time, and CW to CCW counts as a stop and a start (`RuntimeEstimator.AddSpindleChange`); where continuous mode brings the path to rest: at a dwell, a spindle start or stop, a tool change, a STOP, the end of a program and a motion not timed, every other junction a corner, a block without motion between two motions and a reversal of direction included (`RuntimeEstimator`). Cited, not asked again: wave-2 questions #11 (the cycle dwell moves nothing), #25 (HOME is a MOTION), #26 (no TOOL_END for the last tool: its use stays open until the next TOOL_BEGIN on its holder or the end of the run), #27 (CYCLE_CALL, then its motions), #29 (a block counts under the tool of the holder called last), #40 (header row and CSV quoting), #42 (what is written when an ERROR stops the run: the reports of the blocks before it after a line that says so), #46 (the spindle of a feed per revolution on a turret with a driven tool spindle).
- Shared files changed: `src/Ncx.Cli/Program.cs` (the analyze command and `Analytics()` with the two registration lines), `src/Ncx.Cli/Pipeline.cs` (an overload that makes the listeners for the machine of the run once it is loaded; the old signature calls it), `src/Ncx.Cli/RunMachine.cs` (`FileName`, the machine file the reports name), `src/Ncx.Cli/Commands/RunOptions.cs` (`SkipBlocksOption()`, a public factory like `StrictOption()`), and the READMEs of `src/Ncx.Cli`, its `Commands`, `tests/Ncx.Acceptance`, its `Cli` and `Expected`.
- Document fix: architecture 10, the analyze row names its options and its output (F28).

Done when:

- The tool list of `2.5D_FRAESEN.ncx` names two tools with the right distances: holds as the phase file reads it: one row, tool 1, cutting 550.856 mm and rapid 113.907 mm, both computed by hand from the example in `ToolListAnalyticTests`, with the 4 motions of unknown length, 40 blocks, rpm 1592; `INCREMENTAL_SUB` names tools 2 and 3 (tool 3 preloaded). Reference `Expected/analytics/2.5D_FRAESEN.tools.txt`.
- The runtime of `3D_FRAESEN.fanuc.nc` (converted) within 10 percent of the maintainer's measured value: waits for that value, which is not known yet. The estimate on `fanuc-mill-30i` is 191.5 s (0:03:12) and is committed as the unverified reference `Expected/analytics/3D_FRAESEN.fanuc-mill-30i.runtime.txt`, with a `TODO` in `AnalyzeCommandTests` to assert the 10 percent once the measured time is known; the report says it is an estimate. The dynamics of `fanuc-mill-30i.toml` are plausible values, not verified on a machine (D100). The task file therefore stays in `inbox/`.

Gate: `dotnet build -warnaserror` with 0 warnings, 3444 tests passing and 3 skipped (318 in `Ncx.Acceptance`), `dotnet format --verify-no-changes` clean.

Claude (agent), 2026-09-14, P4-02 review fixes. The branch still merges cleanly onto `main` (at e7456a3), so it was not rebased.

- Finding (blocking): the runtime estimate took the moving axes of a motion from its end points only. A full circle (start = end, VM 3.2) therefore moved no axis: max_feed did not limit its feed and it had no acceleration. A half circle that ends on the coordinate it started from on one plane axis ignored the limits of that axis. Both go against VM 8 ("limited by the `max_feed` of every axis that moves", "the smallest `acceleration` of the moving axes"). Fixed in `MovingAxes.Of`: on an ARC that resolved (its center known, its sweep not 0) both plane axes, the keys of its center, are moving axes, as well as every axis whose end points differ. The plane axes then pass the same rotary rule as every other moving axis, so a C of the polar or cylinder plane stays a length (VM 3.1, D102).
- Tests first, in `RuntimeAnalyticTests`, on the test mill whose Y has half the acceleration of X: `MovingAxes_FullCircleAboveTheMaxFeed_MovesAtTheMaxFeedWithTheAccelerationOfY` and `MovingAxes_HalfCircleEndingOnTheYItStartedFrom_AcceleratesWithTheAccelerationOfY`. Before the fix they failed with 0.31416 s and 0.41416 s; after it they pass with the times computed by hand, 0.82832 s and 0.51416 s. The reviewer's reproduction (`fanuc-mill-30i.toml` with X max_feed 1000, a full circle at F=6000 through `ncx analyze --analytic runtime`) now gives 3.8 s, the same as the circle written as two half arcs, where it gave 0.6 s.
- References unchanged: 3D_FRAESEN on `fanuc-mill-30i` stays at 191.5 s, because its arcs are R-form arcs (no full circle, VM 3.2) and X and Y of that machine have the same max_feed and acceleration. The tool list of 2.5D_FRAESEN runs without `[dynamics]`, where the fallback applies no axis limit. No open question arose.
- Done when: unchanged. The tool-list criterion holds; the 3D_FRAESEN criterion waits for the maintainer's measured cycle time, so the task file stays in `inbox/`.

Gate: `dotnet build -warnaserror` with 0 warnings, 3446 tests passing and 3 skipped (320 in `Ncx.Acceptance`), `dotnet format --verify-no-changes` clean.

Claude (agent), 2026-09-14, P4-02 review fixes, second round. The branch still merges cleanly onto `main` (at 77035c1), so it was not rebased.

- Finding (blocking): the runtime estimate timed a RETRACT at the active cutting feed. `RuntimeEstimator.SpeedOf` gave the rapid rate to RAPID and HOME only; a RETRACT went through `CommandedFeed`, which reads the F its MOTION carries, the active F of the program, and before any F it was not timed and was counted as a motion without a known feed. VM 3.1a makes "A `RETRACT` with a feed" an ERROR, so the retract has no feed of its own, and the tool list of the same run counted its distance as rapid: one `ncx analyze` contradicted itself. Fixed: a RETRACT is timed at the rapid rate of its moving axes with the same profile as RAPID and HOME, and without `[dynamics]` as distance over that rapid rate, the reading of the task's scope ("rapid at the axis maximum") and of the tool list. VM 8 names only RAPID and HOME for the rapid rate, so the reading is marked `TODO(question)` in `SpeedOf` and reported. The fallback line of the runtime report now reads "RAPID, HOME and RETRACT distance over the rapid rate"; no reference file contains that line.
- Tests first, in `RuntimeAnalyticTests` on the test mill (Z with rapid 6000 mm/min and 1000 mm/s^2): `Retract_AfterALineWithAFeed_MovesAtTheRapidRateOfTheToolAxis` (LINE X=100 at F=600, then RETRACT=50: 10.01 + 0.6 s), `Retract_WithoutAnyFeedBeforeIt_IsTimedAtTheRapidRate` (0.6 s, and no motion without a known feed) and `Retract_MachineWithoutDynamics_IsDistanceOverTheRapidRate` (10 + 0.5 s, and the report line). Before the fix they failed with 15.02 s, 0 s and 15 s. The reviewer's reproduction on `fanuc-mill-30i.toml` (RETRACT=50 after a LINE at F=100) now gives 6.7 s where it gave 36.5 s, and the tool list of the same run gives the same 6.7 s with the 50 mm under rapid (223.205 mm).
- References unchanged: the Fanuc source of 3D_FRAESEN gives no RETRACT, so its estimate on `fanuc-mill-30i` stays at 191.5 s; the tool list of 2.5D_FRAESEN counted RETRACT as rapid already.
- Open question, marked `TODO(question)` in `RuntimeEstimator.SpeedOf`: at which speed a RETRACT moves in the runtime estimate. VM 8 names only RAPID and HOME for the rapid rate, and VM 3.1a gives the RETRACT no feed. It is not among D108 to D182 or the wave-2 questions.
- Done when: unchanged. The tool-list criterion holds; the 3D_FRAESEN criterion waits for the maintainer's measured cycle time, so the task file stays in `inbox/`.

Gate: `dotnet build -warnaserror` with 0 warnings, 3449 tests passing and 3 skipped (323 in `Ncx.Acceptance`), `dotnet format --verify-no-changes` clean.
