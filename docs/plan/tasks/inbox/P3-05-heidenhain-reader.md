# P3-05 Heidenhain reader

Phase: 3 | Milestone: M6 | Depends on: `P3-02`, `P3-04` | Size: L (S: a day or two, M: up to a week, L: more)

## Goal

Klartext into canonical NCX, per section 7 of `controllers/heidenhain.md`.

## Scope

- Tokenizer for numbered blocks, `~` continuations, the comma, `;` and `* -`, `/`.
- `L`, `C`/`CC`, `CR`, `CT`, `LP`/`CP` with the pole, `LN` vectors, `IX`.. prefixes, `FMAX`/`F`/`F AUTO`, `RL`/`RR`/`R0`, `M91`, `TOOL CALL` split into words, `TOOL DEF`, cycle definitions into absolute coordinates, `CYCL CALL`/`M99`/`CYCL CALL PAT` into calls with `CYCLE=OFF` before the next motion, `CYCL DEF 7`/`8`/`10`/`247`/`19`/`32`/`9`, `PLANE` forms, `M128`/`M129`, `M126`/`M127`, `M116`/`M117`, `M140`, `M136`/`M137`, `LBL` as `SUB` or `LABEL` by usage, `CALL LBL REP`, `CALL PGM`, `FN 0`..`FN 12`, formulas, `Q` names kept.
- `BLK FORM`, `APPR`/`DEP`, `TCH PROBE`, `FN 16`/`18`/`19`, OEM cycles: `RAW`.

## References

- controllers/heidenhain.md section 7
- controller-mapping.md (Heidenhain column)
- examples/sources/*.h

## Done when

- `ncx convert 2.5D_FRAESEN.h --machine heidenhain-itnc530` equals `examples/2.5D_FRAESEN.ncx` (with the WARNING about the missing `M30`).
- Every Heidenhain file of the corpus converts without a crash; `PLANE AXIAL`, `CP IPA`, `LN`, `M140` read into their words.

## Log

(who, when, what was decided while doing it; a decision that changes the specification gets a row in `../../../decisions/decisions.md`)

- Claude (agent), 2026-09-13: section P3-05 of `13-phase-3-readers-compilers.md`, the reader, in `src/Ncx.Readers/Heidenhain/` (namespace `Ncx.Readers.Heidenhain`) with the tests in `tests/Ncx.Readers.Tests/Heidenhain/` and the acceptance in `tests/Ncx.Acceptance/Examples/HeidenhainReaderTests.cs`; `ncx convert` for both readers is P3-02 part two. Implements D5, D7, D29, D31, D34, D48, D81, D82, D83, D84, D85, D86, D89, D92, D94, D98, D105, D106 (D58 waits, below).
  - Files, one per concern: `HeidenhainTokenizer` (block numbers, `~` continuations, `;` comments, `* -`, `/`, the comma and the dot, formulas and `FN n:` as words), `HeidenhainReader` (the structure of a block for the structure pass, the D34 header, the dispatch by the first words, `CYCLE=OFF` before the next non-cycle motion), `HeidenhainLabels` (the two passes of heidenhain 7 rule 3), `HeidenhainMotion` (`L`, `LN`, `LP`, `FMAX`/`F`/`F AUTO`, `R0`/`RL`/`RR`, `M91`, `IX`), `HeidenhainArcs` (`CC`, `C`, `CR`, `CT`, `CP`, `CP IPA` beyond 360 degrees as `ANGLE`), `HeidenhainToolCall`, `HeidenhainCycles` (`CYCL DEF` of the machining cycles through the catalog, `CYCL CALL`, `M99`, `PATTERN DEF` and `CYCL CALL PAT`), `HeidenhainFrameCycles` (247, 7, 8, 9, 10, 19, 32) with `HeidenhainChain`/`HeidenhainChainEntry`, `HeidenhainPlane`, `HeidenhainFlow` (`LBL`, `CALL LBL`, `REP`, `CALL PGM`, `FN 9`..`FN 12`), `HeidenhainQ` with `HeidenhainExpression` (`FN 0`..`FN 5`, formulas, `Q`/`QL`/`QR` names), `HeidenhainFunctions` (the M functions); helpers `HeidenhainBlock`, `HeidenhainDraft`/`HeidenhainDraftBlock`/`HeidenhainWarning`, `HeidenhainState`, `HeidenhainPoint`, `HeidenhainDefinition`, `HeidenhainNumbers`, and for subprograms `HeidenhainCalls`, `HeidenhainCallerState`, `HeidenhainChanges`. Codes `RDR300`-`RDR302` in `src/Ncx.Readers/DiagnosticCodes.Heidenhain.cs`; the missing `M30` is the `RDR010` of the structure pass, whose `TODO(question)` already says every reader reports it.
  - Decided while doing it (representation, nothing a user or plugin sees beyond what the specification says): `END PGM` is the file frame (one program per file), so the program ends at its `M30` or, without one, after its last block with `RDR010`; `LBL` subprograms after the `M30` are `SUB` sections; the block number is no word; the comments of the lines of one continued block are one comment joined with a blank; a cycle written over several blocks (`CYCL DEF 7.0`, `7.1`, ...) is read as a whole at its `n.0` block, and its other blocks write nothing; a `CC` writes no block, its pole goes into the `CENTER` of the arcs after it; an expanded `PATTERN DEF` writes no block; `PLANE RESET` and a zero cycle 7, 8, 10 or 19 where nothing of the kind is active write the `RESET` all the same; an `LBL` that only `JUMP=END` jumps reach writes nothing; an `LBL` that is neither a subprogram nor a label target stays `RAW` (so a `RAW` `CYCL DEF 14` still finds it); a label that a jump or a repeat enters, a `RAW` block, a call and a frame change make the positions (and the pole, the cycle state where needed) unknown to the reader; a subprogram starts from the facts every recorded call agrees on (plane, pole, definition, cycle state, chain, TCPM), and a caller continues with what the subprogram may change unknown (`HeidenhainChanges`); after `CALL PGM` the positions and the pole are unknown, the rest follows the STATIC model of virtual machine 1; a catalog cycle whose parameters are not all mapped to words of the language is written natively, `CYCLE:HEIDENHAIN=n` (as `cycles/heidenhain.toml` says); `M13`/`M14` go through the machine's tables like every M function and are `MFUNC` on `heidenhain-itnc530.toml`.
  - Behaviour questions, `TODO(question)` in the code with the least committal reading: `SAFE` against `Q204=5` (the question of the phase file, `HeidenhainCycles.AddRetract`: every definition with `Q204` is `SAFE` with `CYCLE_RETRACT=SAFE`, without `Q204` `CYCLE_RETRACT=CLEARANCE`; controller-mapping 5 unchanged, `Expected/BOHREN.ncx` keeps the Fanuc reading); the `Q` parameters of a drilling definition that no word of its entry carries (`HeidenhainCycles.WarnUnmapped`, not written, `RDR301`; the reader side of wave-1 question #21); `CYCLE=OFF` "before the next non-cycle motion" read as after a call of the cycle, with the definition written again before a later call (`HeidenhainReader.Write`); `F AUTO` (`HeidenhainMotion.ReadFeed`, RAW); the `F` and the name of a `TOOL CALL` (`HeidenhainToolCall.ReadCall`, RAW); the M functions of a positioning block stand in its NCX block (`HeidenhainMotion`, as the Fanuc reader does); a `CC` with one axis (`HeidenhainArcs.ReadPole`, pole unknown); the tool-axis coordinate of a `PATTERN DEF` point (`HeidenhainCycles.PointOf`, RAW unless 0); an `LBL` section called before the `M30`, in a program without `M30`, or also used by `REP` or a jump (`HeidenhainLabels.DecideSubs`, no subprogram, the call RAW); a new cycle 7, 8, 10 or `PLANE` against transforms programmed after the one it replaces (`HeidenhainChain.TryReplace`, RAW); cycle 247 over an active cycle 7, 8, 10 or tilt (`HeidenhainFrameCycles.ReadOrigin`); the `MB` of a `PLANE` read as `RETRACT` in front of the tilt, and `PLANE RESET TURN` (`HeidenhainPlane.Options`); callers of a subprogram in different states (`HeidenhainCalls`, the question P3-02 asks for Fanuc); `NUMBER=1` and the `BLK FORM` comment of `examples/2.5D_FRAESEN.ncx` against heidenhain 7 rule 9 (`HeidenhainReaderTests`). Wave-1 questions cited where the code depends on them: #9, #20, #21, #22, #36, #46, #59, #62, #63, #72, #73, #95, #101.
  - `BOHREN.h` against `Expected/BOHREN.ncx` (the list in `HeidenhainReaderTests`, every other block equal): `PROGRAM=BEGIN` without `NUMBER=1`; the two `BLK FORM` blocks `RAW` after `ORIGIN=1`; every comment line of the Fanuc source is a `* -` `SECTION` of the Klartext; `TOOL=n` with both offsets and `RPM` (D7), so `SPINDLE=CW` without `RPM` and `RAPID Z=5` without `OFFSET:LEN`; `RAPID X=10 Y=10 COMP=OFF` of the first positioning (`R0`); every cycle with `SAFE=5 CYCLE_RETRACT=SAFE` (the `SAFE` question) and `CYCLE_DWELL=0` from `Q211=0` (not on `TAP`, whose cycle 207 has no `Q211`), `CYCLE_F=565.487` from `Q206=565,487` against `F565`, and no `CYCLE_F`/`CYCLE_DWELL` on `TAP`; the `G73` operation is `PECK` (cycle 203, the first catalog entry, wave-1 question #20); the fourth operation is one `CYCLE=PECK` with five calls where the Fanuc source writes the 269 single moves; `RPM=500 FUNC:RIGID_TAP=ON` of `M29 S500` has no Klartext counterpart; `HOME Z` and `HOME X Y` are the `M91` moves `RAPID Z=0 FRAME=MACHINE` and `RAPID X=0 Y=0 FRAME=MACHINE` (2.5D_FRAESEN note 4).
  - Tests: 104 in `tests/Ncx.Readers.Tests/Heidenhain/` (tokenizer; one or more per rule of heidenhain 7: program and frame, tool call, labels and flow, motion and arcs, frames and plane, cycles, M functions), each output formatting to itself and checking without ERROR where it should; acceptance: 2.5D, BOHREN, 3D speed, the three sources formatting to themselves and checking without ERROR, the corpus (skips without `NCX_CORPUS`, and checks that `PLANE AXIAL`, `CP IPA`, `LN`, `M140` read into `TILT_AXIS`, `ANGLE`, `TX`, `RETRACT`).
  - Shared file: `tests/Ncx.Acceptance/Expected/README.md`, one sentence (the Heidenhain reading is compared except the listed blocks).
  - Done when: (1) holds for the reader through the library with its question: `2.5D_FRAESEN.h` with `machines/heidenhain-itnc530.toml` reads into the blocks of `examples/2.5D_FRAESEN.ncx`, comments and trivia stripped, with the `RDR010` WARNING about the missing `M30`, except `NUMBER=1` and the two `BLK FORM` `RAW` blocks (and their `RDR001` WARNINGs); the command `ncx convert ... --machine heidenhain-itnc530` waits on P3-02 part two. (2) the corpus test exists and skips; it runs when the maintainer sets `NCX_CORPUS`. `3D_FRAESEN.h` reads in a few milliseconds. The task file stays in `inbox/`.
  - Open work, a `TODO` in the code: `CHF` and `RND` are kept `RAW`; D58 wants them expanded into `LINE` and `ARC` blocks as the Fanuc reader does for `,C` and `,R` (not in the file list of the phase plan).
- Claude (agent), 2026-09-13: review fix (commit "P3-05 review fixes"). A `CYCL DEF` the reader keeps `RAW` for a reason other than a catalog miss now replaces the active definition too, as heidenhain 5 says ("The definition stays active until the next `CYCL DEF`"). That covers the cycles written over several blocks other than 7, 8, 9, 10, 19 and 32 (`CYCL DEF 12.0 PGM CALL`, the older `1.0 TIEFBOHREN` ... `1.5`) and a `CYCL DEF` whose cycle number does not read. Before, a later `CYCL CALL` or `M99` called the definition before it, or reported a false `RDR302`; now it stays `RAW` with the definition, without `RDR302`.
  - Decided while doing it (representation): the three `RAW` paths share `HeidenhainCycles.Activate`, and like the catalog miss they leave the NCX cycle state (`CycleOn`, `CycleCalled`) as it was, since a `RAW` definition writes no `CYCLE` block. The subprogram scan `HeidenhainCalls.AddChanges` classifies a `CYCL DEF` the same way through `HeidenhainCycles.FrameCycleOf`, so a subprogram with an unreadable `CYCL DEF` leaves its caller's definition unknown. Read literally, the sentence of heidenhain 5 makes cycles 12, 13, 14 and 20 end the active definition as well; the reader follows it (no `TODO(question)`, the document answers).
  - Tests: `CyclDef_KeptAsRawOverSeveralBlocks_ReplacesTheDefinitionAndItsCallsStayRaw`, `CyclCall_OfAnOlderCycleOverSeveralBlocks_IsKeptAsRawWithoutAnError`, `CyclDef_WithoutACycleNumber_ReplacesTheDefinitionAndItsCallsStayRaw` (`HeidenhainCycleTests`), `M99_AfterASubprogramWithACyclDef_IsKeptAsRaw` (`HeidenhainFlowTests`).
  - Done when, unchanged: (1) holds for the reader through the library, and the command waits on P3-02 part two; (2) holds, with the corpus test skipping until `NCX_CORPUS` is set. The task file stays in `inbox/`.
- Claude (agent), 2026-09-13: review fix, second round (commit "P3-05 review fixes"). D58 for Heidenhain: `CHF` and `RND` are expanded into the explicit `LINE` and `ARC` blocks they stand for (language 4.3), in `HeidenhainCorners` with `HeidenhainCorner`, between two straight lines as the Fanuc reader expands `,C` and `,R` (`FanucCorners`). The line before the corner (`L` or `LP` at the feed in the working plane) ends where the corner begins, its plane words written absolute; the `CHF` block writes the `LINE` to the end of the chamfer, the `RND` block the `ARC=CCW` or `ARC=CW` with `R` tangent to both lines; the `L` after it is read from the end of the corner, its `IX` words less the way the corner went. The open work of the first entry is done, and the reader implements D58.
  - Decided while doing it (representation): the line before a corner prepares it once the whole line is read, looking ahead with `HeidenhainState.NextBlock`, and the `CHF` or `RND` block writes it, so the corner block carries the line of its own source block and its comment; a rounding between two lines in one direction writes nothing (as `FanucCorners`). A corner stays `RAW`, the lines about it as the source writes them, where the reader does not expand it: after `FMAX`, an arc, `LN`, `M91`, an `M99` call or an `M140` retract; before anything but an `L` at the feed in the plane with numbers (an arc, `LP`, `FMAX`, `F AUTO`, a tool-axis word, a `Q` parameter); from a point the reader does not know; with a block skip on one of its three blocks; before a line that changes the radius compensation; before an incremental line with an M function (a reader rule could take that block, D66); where the corner is longer than a line of it or the lines run back on each other. A rounding next to an arc stays `RAW`, as the Fanuc reader keeps `,R` next to an arc.
  - Behaviour questions, `TODO(question)` in the code with the least committal reading: what the length of `CHF` measures (the way along each line from the corner point, as the Fanuc `,C`; `HeidenhainCorners.Expand`); what an `F` in a `CHF` or `RND` block feeds (the block stays `RAW` and the line before it is not shortened; `HeidenhainCorners.Size`); where the `IX` of the line after a corner counts from (the corner point, as the Fanuc reader counts the `G91` line after `,C` and `,R`; `HeidenhainCorners.FromCornerEnd`).
  - Tests (`HeidenhainMotionTests`): `Chf_BetweenTwoLines_IsExpandedIntoALine`, `Rnd_BetweenTwoLines_IsExpandedIntoAnArcTangentToBoth` (CCW and CW), `Rnd_BeforeAnIncrementalLine_WritesItsIncrementalWordFromTheEndOfTheArc`, `Chf_BeforeAnIncrementalLine_WritesItsIncrementalWordFromTheEndOfTheChamfer`, `ChfAndRnd_OnOneContour_AreExpandedWithoutRaw` (the probe of the review: `LINE X=8 Y=0`, `LINE X=10 Y=2`, `LINE X=10 Y=7`, `ARC=CCW X=7 Y=10 R=3`, no `RDR001`), `Rnd_BetweenTwoLinesInOneDirection_WritesNothing`, `ChfOrRnd_WithItsOwnFeed_IsKeptAsRaw`, `ChfOrRnd_WhereTheReaderDoesNotExpandTheCorner_IsKeptAsRaw` (seven cases), `Corners_CheckWithoutError`. No example source holds `CHF` or `RND`, so the acceptance files are unchanged.
  - Done when, unchanged: (1) holds for the reader through the library, and the command waits on P3-02 part two; (2) holds, with the corpus test skipping until `NCX_CORPUS` is set. The task file stays in `inbox/`.
