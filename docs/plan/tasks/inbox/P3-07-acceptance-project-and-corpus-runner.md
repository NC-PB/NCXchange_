# P3-07 Acceptance project and corpus runner

Phase: 3 | Milestone: M6 | Depends on: `P3-06` | Size: M (S: a day or two, M: up to a week, L: more)

## Goal

The tests that hold the whole chain together, and the tool that runs a reader over hundreds of files.

## Scope

- `tests/Ncx.Acceptance`: for every pair in `docs/spec/examples/sources/`, convert from both sources, compare with the `.ncx`, compile back to both, compare with the sources under the comparison rules; the five `.ncx` examples format clean (no machine file, D91) and check with no ERROR without and with their machine files (D103).
- A comparison that ignores block numbers, number formatting within the machine's decimals, comment placement and blank lines, and shows a diff on failure.
- `ncx convert --batch <folder>` (or a test-only runner) that converts every file of a folder, never stops on an error, and writes a summary: files, blocks, `RAW` blocks per word, diagnostics per rule.

## References

- architecture.md section 11 (testing)
- code-guidelines.md section 8
- controllers/sample-corpus.md section 3

## Done when

- The acceptance project is green.
- The batch runner over the maintainer's corpus reports zero crashes for the Fanuc and Heidenhain families (run by the maintainer; the summary is committed as a text file under `tests/corpus-reports/`).

## Log

(who, when, what was decided while doing it; a decision that changes the specification gets a row in `../../../decisions/decisions.md`)

- Claude (agent), 2026-09-18: section P3-07 of `13-phase-3-readers-compilers.md` in full: the round-trip matrix `tests/Ncx.Acceptance/RoundTrips.cs`, the one comparison `tests/Ncx.Acceptance/NcComparer.cs` (with `NcBlock`, and `NcException` moved beside it), `ncx convert --batch <folder> --machine <name> --report <file>` in `src/Ncx.Cli/Commands/BatchCommand.cs` (with `BatchFile`, `BatchOutcome`, `BatchReport`), `tests/corpus-reports/README.md`, the tour `docs/reading-the-code.md` through `convert` and `compile`, and the comparison rules in code-guidelines 8. Codes `CLI350` to `CLI352` in `src/Ncx.Cli/DiagnosticCodes.Batch.cs`.
  - `NcComparer` replaces `NcEquivalence` of P3-04 and the comparison `FanucCompilerTests` kept for itself (P3-06); both compiler tests now call it, and `NcComparerTests` holds the hand-written pairs (equal, block numbers that differ, comments and blank lines, `70.` against `70.0` against `70`, `-.534` against `-0.534`, the `~` lines of a block, the header as sets of words, a `T M6` and a `TOOL CALL` as sets of words and a motion block in order, the skip and its switch on Fanuc and on Klartext, the `;` that is no comment on Fanuc, the unified diff, the exceptions). The `SiemensEquivalence` of P5-02, not on `main` yet, is left to that task to fold into `NcComparer` when it lands.
  - Decided while doing it (the comparison is test code that no user or plugin sees): comments by the controller of the machine, `( )` on Fanuc and nothing else (controllers fanuc.md 1, as `FanucTokenizer` reads them, an unclosed one to the end of the line), `;` outside quotes and the `* -` structuring blocks on Klartext and Siemens, so that the parentheses of a Klartext formula stay; the skip mark is taken first and never counts as a block number (`/` or `/1`..`/9` on Fanuc, `/` on Klartext, `/` or `/0`..`/9` on Siemens, controller-mapping 1, SKIP), then a block number, `N10` on Fanuc and Siemens or the leading number of Klartext, is removed, and the mark, before or after the number, is kept in front of the words without a blank, so that `/2 G0` equals `/2G0`, `/1` differs from `/2` and a skipped block never equals an unskipped one; the header block is the program start (`%`, `O`, `BEGIN PGM`) and the lines of G codes alone that follow it; a tool change is a `TOOL CALL` line or a line with a `T` word and `M6`; on a difference the diff shows one hunk, the blocks that agreed before it, the blocks of each side up to where the two agree again for three blocks (looked for within 50 blocks of each), and the lines of the files where it starts.
  - The comparison rules take the sentence of wave-1 question #12, answered by D49: the `program_end` line is not a difference where the source ends in another form (`M2`, `M30`, `M17`) or in none. `NcComparer` implements it, so the `M30` exceptions of `HeidenhainCompilerTests` are gone; code-guidelines 8 carries it with the rules. The phase file itself is the bookkeeping's (BK-01).
  - `RoundTrips` is the matrix of M6 through the command line (`ncx convert`, `ncx compile`, `ncx format --check`, `ncx check` with the machines by name): 2.5D_FRAESEN from both sources against the example except the readings its notes list (D217, the lists of `FanucReaderTests` and `HeidenhainReaderTests`); BOHREN and 3D_FRAESEN from the Fanuc source byte for byte against their frozen files, from Klartext against them except the listed readings (`HeidenhainReaderTests.BohrenReading`, and for 3D_FRAESEN the list in `RoundTrips`, the kinds of D217); every mill source converted and compiled back to its own controller; the example to both controllers and the frozen BOHREN and 3D_FRAESEN to Klartext (the Klartext readings keep `BLK FORM` as `RAW:HEIDENHAIN`, which compiles to Klartext only, D5, D94, so the frozen Fanuc readings stand for them on the Fanuc side); the five examples format clean and check without an ERROR without and with their machine files; the Nakamura pair converts and checks as single files. The cells the reader and compiler tests hold for their milestones run again here with the lists those tests keep (made `internal`: `HeidenhainCompilerTests.Differences25DFraesen`, `BohrenDifferences`, `WithoutRigidTap`, `StockDefinition`, `Cycle203`, `s_programStart`; `FanucCompilerTests.s_movesOfNote4`; `HeidenhainReaderTests.BohrenReading`; `ConvertExampleTests.ExampleBlocks`, `BlocksOf`), so every list of differences stands in one place.
  - The round trips of the Klartext sources come back but for two differences, both on record: the `M137` of the header (the question of the target state at the start of a program, P3-04) and, for BOHREN, `Q213=24` and `Q213=3` of cycle 203 written back as `Q213=0`, since the reader reads every 203 as `PECK` (D163, wave-1 question #20). 3D_FRAESEN compiled from the Fanuc reading to Klartext differs from `3D_FRAESEN.h` in `M137` and the two `BLK FORM` blocks (D217). The Fanuc round trips come back without a difference.
  - New expected file: `tests/Ncx.Acceptance/Expected/3D_FRAESEN.ncx`, the Fanuc reading of `3D_FRAESEN.fanuc.nc` with `fanuc-mill-30i`, reviewed against the rules the Fanuc reading of 2.5D_FRAESEN follows (the two sources share header and ends; 834 source blocks, 375 arcs for 316 `G2` and 59 `G3` blocks); `Expected/README.md` says so.
  - The batch: every file of the folder is converted as `ncx convert` converts one program (`ConvertCommand.Convert`, extracted from `Run` for both), the canonical text written in memory only; the batch never stops: a file that cannot be read is `CLI002` on the file, a crash (an exception of a reader, the writer, the expander or the virtual machine) the ERROR `CLI351` with the name and message of the exception, and both go on with the next file. The standard error lists the diagnostics of every file as convert reports them (D98); the report holds counts, codes and the names of the files only, never source text (implementation 13, risks): the machine, the files by outcome, per file its result, blocks, `RAW` blocks per word and diagnostics per code, and the totals with the files each word and code stands in. Exit code 2 before the first file for a folder that cannot be listed (`CLI350`) or a missing or unreadable machine (`CLI250`, `CLI200`), otherwise 1 when the machine, a file or the report has an ERROR or a WARNING under `--strict` (D97); a file that cannot be read inside the folder is an ERROR of the run, not exit 2, since the run has started. `--batch` needs `--report` and takes neither a program nor `--output`, `--report` needs `--batch`: usage errors, `CLI001`.
  - Followed the recommendations on record, cited at their `TODO(question)`: D209 (the batch writes its report where the command line names it and no NCX file); D229 (a controller program that is no UTF-8 text is read as Windows-1252 with the WARNING `CLI352`, the byte order mark left out, in `src/Ncx.Cli/ControllerProgram.cs`, which `ncx convert` uses alone and with `--batch`, so that the report says what convert does with each file; `Convert_ProgramThatIsNoUtf8_ExitsTwoWithCli002` became `Convert_ProgramThatIsNoUtf8_IsReadAsWindows1252WithAWarning`, and a program with a byte order mark is tested as well); D217, D163 in the lists of differences.
  - Behaviour questions, `TODO(question)` in the code with the least committal reading: (1) which files `--batch` converts (`BatchCommand.Run`): "every file of a folder" is read as every file of the folder and of its folders, whatever its name (the README of the example sources included), sorted by path, each with the reader of the machine's controller, and the plugins of the working directory are not loaded, since a plugin reports on the one file it was loaded with; (2) the form of the report and what "RAW blocks per word" counts (`BatchReport.Write`): aligned text, the files by path, the words and codes in ordinal order, a `RAW` block counted by its `RAW` word (`RAW:FANUC`, `RAW:NAKAMURA`), since the report holds no source text.
  - The batch over `docs/spec/examples/sources/`: `tests/corpus-reports/examples-sources.fanuc-mill-30i.txt` and `examples-sources.heidenhain-itnc530.txt`, nine files each, none crashed; each reader keeps the programs of the other family as `RAW` without a crash. `BatchCommandTests` writes them when they differ and fails, as the tests of the generated documents do.
  - The timing tests of the chain over 3D_FRAESEN (`FanucCompilerTests`, `FanucReaderTests`, `HeidenhainReaderTests`) run in the xUnit collection `ChainTiming`, alone after the other tests of the assembly: beside the round trips and the batch they took more than their second under a parallel `dotnet test` (two of three runs of `Chain_3DFraesen_TakesWellUnderASecond`; a full run on `main` before this task failed one test of the assembly once as well).
  - Tests: 17 in `tests/Ncx.Acceptance/NcComparerTests.cs`, 36 cases in `RoundTrips`, 16 in `tests/Ncx.Acceptance/Cli/BatchCommandTests.cs`, 2 in `ConvertCommandTests`; `ReadingTheCodeTests` checks the route of the tour on through `ConvertCommand.cs`, `ReaderBase.cs`, `CompileCommand.cs` and `CompilerBase.cs`.
  - Shared files: `src/Ncx.Cli/Commands/ConvertCommand.cs` (`--batch`, `--report`, `Convert`, `ReaderOf` and `ReportMachineRequired` internal, `ControllerProgram`), `src/Ncx.Cli/DiagnosticCodes.cs` (the range line), `src/Ncx.Cli/README.md`, `src/Ncx.Cli/Commands/README.md`; in `tests/Ncx.Acceptance/`: `Examples/HeidenhainCompilerTests.cs`, `FanucCompilerTests.cs`, `HeidenhainReaderTests.cs`, `FanucReaderTests.cs`, `ConvertExampleTests.cs`, `Cli/ConvertCommandTests.cs`, `Repository/ReadingTheCodeTests.cs` and the READMEs of the project, `Examples/`, `Expected/` and `Cli/`. `src/Ncx.Cli/Program.cs` is unchanged: `--batch` is an option of `convert`. Document edits: code-guidelines 8 (the comparison rules), architecture 10 (the row of `convert --batch`, F28), `docs/reading-the-code.md`.
  - Done when: (1) holds: the acceptance project is green (`dotnet test`, 552 passed, the 6 corpus tests skipped without `NCX_CORPUS`). (2) waits for the maintainer: the batch over the corpus with the Fanuc and the Heidenhain machine, the reports committed as `tests/corpus-reports/corpus.<family>.<machine>.txt` with `crashed 0` (the commands are in `tests/corpus-reports/README.md`). The task file stays in `inbox/`.
- Claude (agent), 2026-09-18, review fixes: `NcComparer` read `;` as a comment on Fanuc and cut it before the `( )` comments, so that `N10 G1 X1. (T1;D10) Y2.` equalled the same block with `Y3.`; on Fanuc it now removes the `( )` comments only, as controllers fanuc.md 1 says (nothing else is a comment) and `FanucTokenizer` reads, and cuts at `;` on Klartext and Siemens only. Its block-number pattern read the switch of a skip as a block number (`/2 G0 X2.` became `/G0 X2.`, equal to `/1 G0 X2.`; `/12 L` of Klartext became `/1L`); the pattern is now one per controller, the skip mark taken first, so `/1` against `/2` is a difference and `/2 G0` against `/2G0`, the form the Fanuc compiler writes, is none. Three new tests in `NcComparerTests` (`Compare_SkipSwitchOnFanuc_IsNoBlockNumber`, `Compare_SkipOnKlartext_TakesNoDigitOfTheBlockNumber`, `Compare_SemicolonOnFanuc_IsNoComment`); the round trips are unchanged, the example sources have neither a `;` on Fanuc nor a skip with a switch.
