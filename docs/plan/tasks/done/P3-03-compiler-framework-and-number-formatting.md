# P3-03 Compiler framework and number formatting

Phase: 3 | Milestone: M5 | Depends on: `P3-01` | Size: M (S: a day or two, M: up to a week, L: more)

## Goal

What every compiler shares: walking the program with the STATIC VM, deciding what the target needs written, formatting numbers, writing files.

## Scope

- `ICompiler` (`Compile(NcxProgram, MachineConfig) -> CompiledFiles`), a `TargetState` (what the target control has active, so a modal G is written only on change), the `BLOCK_WRITE` event and the `IBlockWriter` interface in `Ncx.Compilers`, where its caller is (D106), each `SUB` section emitted once, not once per `CALL`, although the STATIC walk follows every `CALL` (D99): the section is written from an unknown target state, so every modal word stands at its first use inside it and the text is right for every caller, and walks that would write different lines are an ERROR naming the section and the calls; under `program_layout = "file_per_program"` (Heidenhain) the section is written once per calling program (VM 3.9, language 4.13), the output layout (`out/<machine>/`, `program_layout` one file or file per program, D48).
- Number formatting per `[format]`: decimals per address, trailing zeros, decimal separator, culture-invariant; block numbers; line length; comment charset transliteration.
- The chain writer: frames in program order on every target (D31).
- `RAW` of another controller, and a `CYCLE:<controller>=n` block of another controller family (D94): ERROR at compile time.

## References

- architecture.md section 8 (compilers, sequence diagram)
- machine-config.md section 2
- ncx-language.md section 2 rule 5

## Done when

- A fake compiler writes three blocks with the formatting of two different `[format]` tables.
- `RAW:FANUC` compiles to Fanuc and fails for Heidenhain with the ERROR text of the specification.
- `CYCLE:HEIDENHAIN=251 Q215=0 Q218=60` compiles to Heidenhain with the native parameters in source order and fails for Fanuc with the same ERROR text (D94).

## Log

(who, when, what was decided while doing it; a decision that changes the specification gets a row in `../../../decisions/decisions.md`)

- Claude (agent), 2026-09-14: section P3-03 of `13-phase-3-readers-compilers.md` in full, in `src/Ncx.Compilers/` (namespace `Ncx.Compilers`) with the tests in `tests/Ncx.Compilers.Tests/`, and the command `ncx compile` in `src/Ncx.Cli/Commands/CompileCommand.cs`, on which P3-04 and P3-06 build. Implements D5, D10, D31, D48, D49, D52, D77, D91, D94, D97, D98, D99, D105, D106 and wave-1 question #64.
  - Types. Public, because the CLI registers the compilers and plugins write block writers: `ICompiler` (`Controller`, `Compile(NcxProgram, MachineConfig, CompileOptions)`), `CompileResult` (`Files`, `Diagnostics`), `CompiledFile` (`Name`, `Text`), `CompileOptions` (`Rewriters`, `BlockWriters`, `ToolTable`, `ToolTableFile`), `CompilerRegistry` (keyed by the `Controller` of the machine file, a second registration throws), `IBlockWriter` (`Write(BlockWriteEvent)`, D106), `CompilerBase`, and what a family uses from it: `BlockStep`, `LookAhead`, `TargetState`, `NumberFormatter`, `NativeCycle`, `ChainWriter` with `ChainChange`, `CommentCharset`. Internal: `OutputBuffer`, `OutputLine`, `StepRecorder`, `WriteContext`, `ProgramText`, `SubText`, `PendingPreload`. `CompilerBase` is public as `ReaderBase` is.
  - Template method (`CompilerBase.Compile`): expand with the rewriters of the options; report every `RAW` of another controller or builder and every `CYCLE:<controller>=n` of another family as `CMP001` before anything is written, with one text for both (language 4.1, 4.7.1; controller-mapping 9; D5, D94); run the virtual machine STATIC with `StepRecorder` subscribed; write every step in walk order; lay the files out. A family implements `Controller`, `FileExtension`, `CommentLine` and `WriteBlock(block, before, after)`, which writes with `Line(...)`; `WriteHeader(program, state)` (the state after the program's first block with a verb, where the complete header of D34 stands) and `WriteFooter(program, state)` (after the subprograms placed behind the program, END PGM on Heidenhain) are virtual; `BlockNumberPrefix`, `TakesBlockNumber(line)` and `DecimalPointOnWholeNumbers` say how the controller numbers its blocks and writes a whole number. Helpers: `WriteTemplate`, `WriteProgramEnd`, `WriteSubEnd`, `WriteToolChange`, `WritePreload`, `RawText`, `NativeCycleOf`, `CommentText`, and `Target`, `Numbers`, `Templates`, `LookAhead`, `Step`. An ERROR anywhere stops the compile, and a stopped compile gives no file (virtual machine 2.9, code-guidelines 6).
  - The virtual machine hands the compiler every block (a change in `Ncx.Core`, listed below): under the new `VmOptions.RaiseBlockWrite`, false by default, every executed block raises BLOCK_WRITE last among its events, with its words and no lines yet (architecture 5.3 and 8: the compiler is a listener of BLOCK_WRITE). A block that raises nothing else, a `COMMENT` or a `RAW`, reaches the compiler all the same. check, trace and annotate leave the option off, so no expected event, trace or check file changes. The compiler records every step before it writes a line, so the whole STATIC run is the look-ahead of architecture 8: one run, not two.
  - D99: every walk of a `SUB` is written from a fresh, unknown `TargetState`; the first walk gives the section's text; a walk that writes other lines is `CMP002` on its `CALL`, naming both calls and the first lines that differ; after the return the caller's target state takes over what the walk wrote. `one_file`: the programs in file order, each `SUB` once after the first program that calls it; `file_per_program`: one file per program named after its `NAME`, the subprograms it calls (directly or through another) copied after it, then its footer. Subprograms stand in the order of the `SUB` sections of the NCX file, the order a reader produced from the source, which the round trips of P3-07 compare.
  - Numbers and lines (machine-config 2): `NumberFormatter.Format(address, value, block)` with the decimals of the `[format]` key given (a family passes `X` for its `I`), `trailing_zeros` and the decimal separator (`DecimalSeparatorOf`: the written key, else the comma on Heidenhain and the point otherwise, wave-1 question #64; `TemplateSet` follows in FU-06), in the invariant culture; what fits is never rounded; a value with more decimals than the machine takes is rounded half away from zero (language 4.12, ROUND) with `CMP020`, once per value; zero gets no minus sign. `OutputBuffer`: block numbers per `block_numbers` with the family's prefix on the lines that take one, `max_line_length` counted with the number, `line_ending`. `CommentCharset`: the umlauts under `ASCII`.
  - Tool change (machine-config 3, virtual machine 3.5, D52, D91): `unload` for `TOOL=0`, `change_preloaded` when the tool was preloaded (a bare `TOOL` among them), `change` otherwise; `{tool}`, `{name}`, `{offset}` (the combined register), `{rpm}` (written as `S`), `{axis}` (the tool axis letter of the workplane), `{next}` (a `PRELOAD` of the same holder that follows before the next motion, in the `TOOL` block itself or after it, is folded into the change and not written again; else, with `auto_preload` in a program without `PRELOAD` words, the next tool of the look-ahead; else the tool the virtual machine has preloaded), `{kind}` (tool table and `kind_map`), `{b}` (B after the next motion that names B), `{c}` (the next `ORIENT:TOOL`). `auto_preload` with a change command without `{next}` inserts the preload of the next tool after the change, or before the first motion after it under `preload_position = "before_first_motion"`. `PRELOAD` on a machine without a preload template: `CMP011`. A template the machine file does not give: `CMP010`.
  - D10: `ToolTable` and `ToolData` in `Ncx.Config`, new files, loaded like `VarsFile`; the command reads the file that `tool_table` names next to the machine file (the recommendation of D10); a missing file is no error, one that cannot be read is `CLI402` (exit 2), one with an ERROR stops the run (exit 1). Where `{kind}` takes the default, the program gets three comment lines after its head (the header and the lines of `PROGRAM=BEGIN`) that name the expected file and the tools.
  - `ChainWriter` (D31): `Between(before, after)` gives the removed entries, the last first, and the appended ones in program order; `Write` calls the family's writers for a cancel and for an entry in that order.
  - `ncx compile <file.ncx> --machine <toml> [--output <folder>] [--strict]`: `--machine` required unless `ncx.toml` names the machine (`CLI400`, exit 2, D77); the compiler chosen by the machine's controller from `Program.Compilers()` (`CLI401` when there is none, exit 1; the registry stays empty until P3-04 and P3-06 add one line each); the files into `out/<machine>/` of the working directory, named after the machine file, or into the `out` of `ncx.toml`, or into the folder of `--output`; a file that cannot be written is `CLI004`. `RunMachine` gives the machine file it found and `ncx.toml` for that (two properties added).
  - Codes: `CMP001` NativeTextOfAnotherController, `CMP002` SubprogramWalksDiffer, `CMP003` UncalledSubprogramNotWritten, `CMP010` TemplateMissing, `CMP011` PreloadDropped, `CMP020` MoreDecimalsThanTheMachineTakes, `CMP021` LineLongerThanTheMachineTakes, `CMP022` CommentCharacterOutsideCharset; `CLI400` CompileMachineRequired, `CLI401` NoCompilerForController, `CLI402` ToolTableUnreadable. The families take CMP ranges of their own.
  - Open questions, marked `TODO(question)`: (1) the form of the tool table file (`src/Ncx.Config/ToolTable.cs`); (2) the default `{kind}` of D10, and what the warning block is and when it stands (`CompilerBase.Tools.cs` DefaultKind, `CompilerBase.Layout.cs` ToolDataWarning); (3) what a machine file that leaves out `program_layout`, `line_ending` or the `start` and `step` of `block_numbers` writes, and what a line longer than `max_line_length` does (`CompilerBase.Layout.cs` Files, `OutputBuffer.cs`); (4) where a `SUB` that no program calls goes, under `one_file` and under `file_per_program` (`OneFile`, `FilePerProgram`); (5) the file name of a program without `NAME` under `file_per_program` (`FileNameOf`); (6) what a change to the words of BLOCK_WRITE does (`CompilerBase.Walks.cs` RaiseBlockWrite); (7) `{next}` of the last change under `auto_preload` (`NextOf`); (8) a `comment_charset` that is missing or not ASCII, and characters that are no umlaut (`CommentCharset.cs`); (9) whether `--output` of compile names a folder or a file (`CompileCommand.Run`). The preload placeholders follow the recommendation of D150, and a tool called by name with `{tool}` waits on D172; both are marked with their numbers and not asked again.
  - Done when: all three criteria hold, with the fake compiler of `tests/Ncx.Compilers.Tests/Fakes/`: `CompilerBaseTests.Format_PointTableWithBlockNumbersFromTen_WritesThreeBlocksWithItsNumbers` and `Format_CommaTableWithTrailingZeros_WritesTheSameBlocksWithItsNumbers`; `Raw_OfTheSameController_IsWrittenVerbatim` and `Raw_OfAnotherController_IsTheErrorCmp001AndWritesNoFile`; `NativeCycle_OfTheSameFamily_IsWrittenWithItsParameters` (`CYCLE:HEIDENHAIN=251 Q215=0 Q218=60`), `NativeCycle_ParametersInAnotherOrder_KeepTheirSourceOrder` and `NativeCycle_OfAnotherFamily_IsTheSameErrorAsRaw`. The other tests of the section: `TargetState_ModalWordThatStaysActive_IsWrittenOnce`, `ToolChangeTests` (`{next}` from a following `PRELOAD` and from `auto_preload`), `ToolTableWarningTests` (the warning block); further `SubprogramTests`, `BlockWriterTests`, `NumberFormatterTests`, `OutputBufferTests`, `CommentCharsetTests`, `ChainWriterTests`, `CompilerRegistryTests`, `DiagnosticCodesTests`, `tests/Ncx.Core.Tests/VirtualMachine/Events/BlockWriteEventTests.cs`, `tests/Ncx.Config.Tests/ToolTableTests.cs` and `tests/Ncx.Acceptance/Cli/CompileCommandTests.cs`. No compiler of a family exists yet; the acceptance of M5 and M6 is P3-04's and P3-06's.
  - Shared files: `src/Ncx.Core/VirtualMachine/VmOptions.cs` (`RaiseBlockWrite`), `VirtualMachine.cs` (`RaiseEvents` adds BLOCK_WRITE under the option), `Events/BlockWriteEvent.cs` (remarks); `src/Ncx.Config/ToolTable.cs` and `ToolData.cs` (new); `src/Ncx.Cli/Program.cs` (the compile command and `Compilers()`), `RunMachine.cs` (`MachineFile`, `Project`), `README.md`, `Commands/README.md`; `tests/Ncx.Acceptance/Cli/README.md`; `src/Ncx.Compilers/Placeholder.cs` removed.
  - Document fix (F28): the compile row of architecture 10 names `[--output <folder>]`.
  - Risk for P3-06: the recorded steps keep a Before and an After snapshot of every block of the file; `3D_FRAESEN` is small, the megabyte files of the corpus should be measured there.
- Claude (agent), 2026-09-14, review fix: the look-ahead of the STATIC pass ran past the end of the program, so that a program's `{next}`, its `auto_preload` and its folded `PRELOAD` could come from the next program of the file, and a `PRELOAD` at the head of that program was folded into the change before it and written nowhere (against language 2 rule 8). Every search of `LookAhead` (`FollowingPreload`, `NextTool`, `NextB`, `NextToolOrientation`) now stops at the end of the program of its step: its `PROGRAM=END`, the executed end where the control rewinds, with the walks of the subprograms it calls inside it; a subprogram that no program calls is a program of its own from its `SUB=BEGIN` to its `SUB=END` (machine-config 3; virtual machine 1, 3.5, 3.9; language 4.1, 4.13). "The program has `PRELOAD` words" is counted per program, not over the file: `LookAhead.ProgramHasPreload(step)` is a method now, and the constructor no longer takes the flag (virtual machine 3.5). Open question (10), marked `TODO(question)` in `LookAhead.MarkProgram`: whether the `PRELOAD` words of a subprogram that a program calls are that program's own; they count, so that `auto_preload` inserts no preload beside those the run of the program writes. Tests: `ToolChangeTests.AutoPreload_TwoPrograms_LastChangeOfTheFirstPreloadsNoToolOfTheSecond`, `Next_PreloadAtTheHeadOfTheNextProgram_IsNotFoldedAndIsWrittenThere` and `AutoPreload_ChangeInACalledSub_LooksAheadInTheCallingProgramOnly`. No expected-output file and no shared file changed. All three "Done when" criteria still hold.
- Claude (agent), 2026-09-14, second review fix: `auto_preload` under `preload_position = "before_first_motion"` could write a stale preload and drop the real one. The waiting preload waited only for a motion in its own program or walk, so it outlived a `CALL` whose subprogram did the moving and the next change of its holder, and then stood after that change as a preload of the tool already in the spindle (against virtual machine 3.5, and the WARNING of virtual machine 5). `LookAhead.BeforeFirstMotion` now finds, at the change, the step before whose lines the preload stands: the first motion after the change, or the next change of the holder when that comes first. The preload is dropped when that change brings the tool it names, and stands before the change otherwise (machine-config 3; virtual machine 3.5, 5). It is written into the part of the change or into the caller that part returns to, so a change in a subprogram whose first motion follows the return writes its preload in the caller before that motion, and no longer before the `SUB=END` (the old rule "or for its end" was in no document). Open question (11), marked `TODO(question)` in `LookAhead.BeforeFirstMotion`: when the first motion lies in the walk of a subprogram entered on the way, whose section is written once for every caller (D99), the preload stands before the line of the `CALL` that enters the walk, or right after the change where no line of its part stands between the change and the walk. `PendingPreload` holds the index of that step, and the compiler keeps a list of them, so two holders can each wait; `EndWalk` and `EndProgram` no longer clear it. Tests: `ToolChangeTests.AutoPreload_BeforeFirstMotionWithTheNextChangeFirst_IsDropped`, `AutoPreload_BeforeFirstMotionWithAnUnloadFirst_StandsBeforeTheUnload`, `AutoPreload_BeforeFirstMotionInACalledSub_StandsBeforeTheCall`, `AutoPreload_BeforeFirstMotionWithACallOfASubWithoutMotion_StandsBeforeTheMotionOfTheCaller` and `AutoPreload_BeforeFirstMotionAfterAChangeInASub_StandsBeforeTheMotionOfTheCaller`. No expected-output file and no shared file changed. All three "Done when" criteria still hold.
