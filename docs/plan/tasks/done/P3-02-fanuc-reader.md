# P3-02 Fanuc reader

Phase: 3 | Milestone: M4 | Depends on: `P3-01` | Size: L (S: a day or two, M: up to a week, L: more)

## Goal

Fanuc and ISO programs into canonical NCX, per section 9 of `controllers/fanuc.md`.

## Scope

- Tokenizer for `%`, `O`, `N`, `( )`, `/`, addresses with signed numbers with leading or trailing dots, `#` variables and `[ ]` expressions.
- Modal resolution (groups 01, 02, 03, 05, 06, 07, 08, 09, 10, 12), the G-code system of the machine (`gcode_system`, `U W` addresses in system A).
- Tools per section 5 of the Fanuc document: `T` alone, `M6`, `T4 M6`, lathe `T0656`, `G43 H`, `D`; the `S` binding rule.
- Frames: `G54`.., `G52` with `SHIFT=RESET`, `G53`, `G28`/`G30` as `HOME`, `G50`/`G92` as `SETPOS`, `G68`/`G69`, `G51.1`, `G68.2`/`G53.1` as `TILT` with `MOVE`, `G68.1` as `TILT_AXIS`, `G43.4`/`G43.5` as `TCPM`, `G12.1`/`G7.1`.
- Cycles `G73`..`G89` into `CYCLE=` with absolute coordinates and `CYCLE_CALL` per position, `G80`, `K` repeats; lathe cycles from the catalog.
- Macro B: `#n` to `Vn`, expressions, `IF GOTO`, `WHILE DO`, `GOTO`, `G65` calls with the letter table, `M98`/`M99`, `M99` in the main program as a loop, `/M30`.
- `ncx convert` wired to the reader.

## References

- controllers/fanuc.md
- controller-mapping.md (Fanuc column of every section)
- examples/sources/2.5D_FRAESEN.fanuc.nc, BOHREN.fanuc.nc

## Done when

- `ncx convert 2.5D_FRAESEN.fanuc.nc --machine fanuc-mill-30i` equals `examples/2.5D_FRAESEN.ncx` (M4).
- `BOHREN.fanuc.nc` reads into the expected `CYCLE=` words (expected file added to the acceptance project).
- Every Fanuc/ISO file of the corpus converts without a crash.

## Log

(who, when, what was decided while doing it; a decision that changes the specification gets a row in `../../../decisions/decisions.md`)

- Claude (agent), 2026-09-13, part one, the reader: section P3-02 of `13-phase-3-readers-compilers.md` without `ncx convert`, in `src/Ncx.Readers/Fanuc/` (namespace `Ncx.Readers.Fanuc`) with the tests in `tests/Ncx.Readers.Tests/Fanuc/` and the acceptance in `tests/Ncx.Acceptance/Examples/FanucReaderTests.cs`. Implements D5, D7, D29, D30, D33, D34, D40, D49, D51, D55, D58, D59, D60, D66, D82, D85, D91, D92, D93, D98, D105, D106 (D65 waits, below).
  - Files, one per concern: `FanucTokenizer` (`%`, `O`, `N`, comments anywhere, `/` and `/n`, numbers with a leading or a trailing dot, `#n`, `[ ]`, `IF`, `THEN`, `GOTO`, `WHILE`, `DO`, `END`, `,C`/`,R`, the extended axis names `C2=`; CR LF and LF), `FanucReader` (the structure of a block for the structure pass, the D34 header through `FanucHeader`, the concerns in order), `FanucModalGroups` (fanuc 3 by `gcode_system`), `FanucMacro` with `FanucExpression`, `FanucFrames` with `FanucTilt`, `FanucToolWords`, `FanucCycles`, `FanucMotion` with `FanucCorners` (D58) and `FanucPolarCoordinates` (`G16`), `FanucBuilder` (M codes through the tables, `MFUNC`, `[workpiece]`, `[sync]`, the codes kept as `RAW`); helpers `FanucBlock` (the block being read, every word marked when a concern reads it), `FanucDraft`/`DraftBlock` (the NCX blocks a source block reads into), `FanucState` (the facts of the file), `FanucAxes`/`FanucAxisWord`, `FanucFunctions`/`FanucFunction`, `FanucTemplates` (a machine template matched against the words of a block), `FanucCycle`, `FanucLoop`, `FanucNumbers`. Codes `RDR100`-`RDR105` in `src/Ncx.Readers/DiagnosticCodes.Fanuc.cs`; the Fanuc range is `RDR100`-`RDR299`.
  - Shared file: `ReaderBase` gets `protected virtual void BeginSection(SectionKind kind, SourceBlock begin)`, called after the structure pass has written `PROGRAM=BEGIN` or `SUB=BEGIN` and before the first block of the section is read, so that a family writes the header of D34 there and starts the section's state (a subprogram from the unknown state of its caller). Nothing else of the framework changed.
  - Decided while doing it (representation, nothing a user or a plugin sees beyond what the specification says): a word that no concern reads keeps its block as `RAW`, and a pre-check keeps `G10`, `G31`, `G38`, the codes of `[raw]` and every G code the reader does not map `RAW` before any concern reads, so nothing is dropped (D5); the facts of a file live in `FanucState`, tied to the `SourceState` of its read; the comments of one block are joined with a blank; the D34 header takes its values from the leading header blocks of the program (2.5D_FRAESEN: `N10 G0 G40`, `N20 G80 G90 G94 G98`), else `units_default` and the initial state of the VM, and `DIAMETER=ON` where the X axis is programmed in diameters (controller-mapping 1, D60); the modal words `UNITS`, `WORKPLANE`, `FEED_MODE`, `COMP`, `CYCLE=OFF` are written where the source changes them (the example's `N70 G17` writes no word); positions and written modal words are forgotten at a label a jump enters, at a section begin, at a frame change and after a `RAW` block; an expression is written in the form the expression parser gives it, so the output formats to itself; a program the file does not hold is called as an external program, `CALL="O8889"` (language 4.9); an absurd number (beyond a decimal) keeps its block `RAW` instead of throwing (code-guidelines 4).
  - Behaviour questions, marked `TODO(question)` where the code takes the least committal reading: the 2.5D acceptance against the example's own notes (`FanucReaderTests`); `I J K` as `CENTER:X` from a known start under `G90`, else `CENTER:IX` (`FanucMotion.ReadArc`); a left-out `I` or `J` as 0 (same); the code of group 01 before the source writes one, `RAPID` (`FanucMotion.VerbOf`); the `F` of a cycle block written on the next feed motion (`FanucMotion.AddModalFeed`); `G28` with an absolute value (`FanucFrames.ReadHome`); `G54` over an active `G52`/`G68` (`FanucFrames.ReadOrigin`); an incremental `G52` word (`FanucFrames.ReadShift`); the centre of `G68` (`FanucFrames.ReadRotation`); `G5.1 Q1` (`FanucFrames.ReadTolerance`); `G68.1` (`FanucTilt`); the turret `T` against the Mori Seiki `T9001` preload (`FanucToolWords.Turret`); a `G0` ending the cycle of a mill (`FanucCycles.Ends`); `R` of a lathe drilling cycle (`FanucCycles.ReadDrilling`); the bare `CYCLE_CALL` of a defining block against language 6 (`FanucCycles.Call`); `M99 P` (`FanucMacro.ReadCalls`); the older `M98 P51002` on a 30i (`FanucMacro.CalledProgram`); the datum in the `[workpiece]` template (`FanucBuilder.WorkpieceOf`). Wave-1 questions cited where the code depends on them: #14, #15, #16, #18, #19, #29, #45, #46, #62, #73, #85, #86.
  - Tests: 109 string-in string-out tests of the Fanuc column (controller-mapping 1 to 7 and 9) and of the tokenizer; acceptance: 2.5D, BOHREN, 3D speed, the three mill outputs formatting to themselves and checking without ERROR, the Nakamura pair, the corpus (skips without `NCX_CORPUS`). `tests/Ncx.Acceptance/Expected/BOHREN.ncx` is frozen after the review against controller-mapping 5 (`Expected/README.md`).
  - Done when: (1) the reader half holds with a question: through the library, `2.5D_FRAESEN.fanuc.nc` with `machines/fanuc-mill-30i.toml` reads into the blocks of `examples/2.5D_FRAESEN.ncx`, comments and trivia stripped, except the ten blocks the example itself reads otherwise for Fanuc (notes 2 and 4, D7, D92, the `S` binding rule, the `R0` of Klartext); the command `ncx convert ... --machine fanuc-mill-30i` waits on part two. (2) holds: `BOHREN.fanuc.nc` reads into `Expected/BOHREN.ncx`. (3) the test exists and skips; it runs when the maintainer sets `NCX_CORPUS`. The Nakamura pair keeps as `RAW` only `G411`, `G300`, `G333` (`RAW:NAKAMURA`), `G10` (`RAW:FANUC`, a data setting of the control that `[raw]` does not name) and the three blocks of path 2 that read `#11099`, `#5024`, `#5025`, which `nakamura-ntjx.toml` does not map (no `[system_variables]`; a question for the machine file). `3D_FRAESEN` reads in a few milliseconds.
  - Remaining for part two: `ncx convert <file> --machine <name>` in `Ncx.Cli` (D77; the reader, `NcxWriter`, then the STATIC check of P1-07 with the machine resolution of P2-04 part two). Open work left in the reader, each a `TODO` in the code: the contour of `G70`..`G76` as a `SUB` section with `CONTOUR` (D65; the structure pass moves no block range yet, so the cycle and its contour stay `RAW`), `G16` under `G91`.
- Claude (agent), 2026-09-13, part one, review fixes (the five blocking findings of the review of part one):
  - NT NURSE branch commands (`FanucMacro`): on a machine of the builder `nakamura`, `G480 I{n}` is `JUMP=n`, `G471`..`G476 D Q I{n}` is `JUMP=n IF={D op Q}` and `G481`..`G486 Q R I{n}` is `JUMP=n IF={$Vq op $Vr}`, with op `==`, `!=`, `>=`, `<=`, `>`, `<` in the order of the codes (controller-mapping 6 and 9); the order of `G481`..`G486` is read as the one the same sentence gives `G471`..`G476`. Another word in the block is a macro argument NCX does not know and keeps it `RAW`; on another builder the codes stay `RAW` as unmapped G codes.
  - Labels: `StructureOf` reports the `I{n}` of a `G411` (which stays `RAW:NAKAMURA`) and of a branch command as a used label, so `N{n}` keeps its `LABEL`: in the Nakamura pair 102, 104, 110, 114, 9000, 30, and `LABEL=2` in front of the moved `N2 M2` section. A `GOTO` or branch command to a block that holds only `N` and `M30`/`M2` is `JUMP=END` without a `LABEL` (controller-mapping 1, `JUMP=END`; language 4.9); `FanucState.AddJump`/`AddLabel` decide it while the structure is laid out and take the label back out of the jump's list when the end block comes after the jump (representation, decided).
  - Corners (D58): the incremental plane words of the block after an expanded `,C`/`,R` are written less the way the corner went (`FanucCorners.FromCornerEnd`), so the path ends where the source's does; a next block with a block skip, or an incremental one that a jump enters, keeps the corner block `RAW`.
  - `G70`..`G76` (D65; language 4.7, 4.7.1; machine-config 6): the contour that follows the cycle block, `P`..`Q`, becomes `SUB=BEGIN NAME=CONTOUR_<P>_<Q>` ... `SUB=END` behind the end of its program, and the cycle block `CYCLE=<entry> CONTOUR=<name>` with a `CYCLE_CALL`; the contour words and the parameters come from the catalog entry. Another cycle of the program that names the same range (`G70` after `G71`) shares the section; a name already taken gets `_2`, `_3`. A range that does not follow its cycle or holds a structure word, a jump, a call, a jump target or another contour, and a cycle block with a word its entry does not map (wave-1 question #18: `U`, `W`, `F` of `G71`), keep the cycle and its contour `RAW` as before; the contour blocks are now looked for in the cycle's own program. The section name `CONTOUR_<first>_<last>` is a representation choice (decided).
  - Shared-file changes (the framework of P3-01, kept minimal, explained like `BeginSection`): `SourceStructure.Contour` (new public record `SourceContour`) and `SourceStructure.Channel`; `StructurePass.Plan` gives the new internal `ContourLayout` as an out parameter, the new partial `StructurePass.Contours.cs` finds, moves and writes the contour sections, `Steps` leaves a moved block out of its place, `WriteAtBoundary` uses the new `Boundary`, `ProgramBegin` writes `CHANNEL`; `ReaderBase` keeps the layout and gives the family `ContourOf(SourceBlock)` and `InContourSection(SourceBlock)`; one clause in `src/Ncx.Readers/README.md`.
  - `CHANNEL` (controller-mapping 7; language 4.14): a file named `O1000.P-2` on a machine of the builder nakamura reads with `CHANNEL=2` on `PROGRAM=BEGIN`. `M92`, the `PHASE` of `[spindle_sync]`, stays `RAW` instead of `MFUNC` (a table names it, and `PHASE` needs an angle the code does not carry).
  - Tests: `G85_WithG99_BecomesReamCycle`, `G86_WithG98_BecomesBoreCycleBackToTheInitialLevel`, `T0M6_BecomesTool0`, `GotoToTheM30Block_IsJumpEnd`, `SlashM30_IsSkipJumpEnd`, `M92_PhaseCodeWithoutAnAngle_IsKeptAsRaw`, `Channel_NakamuraFileOfPath2_IsChannel2`, `Channel_FileWithoutThePathSuffix_WritesNoChannel`, `G480_OfTheBuilderNakamura_IsAJumpToTheLabelOfI`, `G471ToG476_CompareDWithQ_AreJumpsWithIf`, `G481ToG486_CompareTheVariablesOfQAndR_AreJumpsWithIf`, `G480_OnAMachineOfAnotherBuilder_IsKeptAsRaw`, `G411_TheBlockOfItsJumpTarget_KeepsItsLabel`, four corner tests (`G91` after `,R` and after `,C`, a skipped next line, a next line a jump enters), the theory `G70ToG76_WithTheContourThatFollows_AreTheCatalogCycleWithTheContourAsASubSection` (one case per catalog entry, formats to itself, checks without ERROR), `G71AndG70_OfOneContour_ShareItsSubSection`, `G70_WithAContourThatDoesNotFollowIt_IsKeptAsRawWithItsContour`, and the acceptance `Convert_NakamuraPath_KeepsTheLabelOfEveryG411Target`.
  - New behaviour questions, `TODO(question)` in the code: the path of a file beyond the Nakamura `.P-n` suffix (`FanucReader.ChannelOfFile`), the `PHASE` of `M92` (`FanucBuilder.ReadCode`), `CYCLE=OFF` after a one-shot cycle (`FanucCycles.ReadContourCycle`).
  - Done when: as above, (1) holds for the reader half with its question, (2) holds, (3) the test exists and skips without `NCX_CORPUS`. Remaining for part two: `ncx convert` in `Ncx.Cli` (D77, with P1-07 and P2-04 part two). Open work left in the reader: `G16` under `G91` (a `TODO` in the code); the contour item of the line above is done.
- Claude (agent), 2026-09-13, part one, second review fixes (the three blocking findings of the second review of part one):
  - The chain of transforms (language 4.2, D31; controllers fanuc.md 4): the reader keeps the chain it writes entry by entry (`FanucChain`, `FanucChainEntry`: the `SHIFT` of `G52`, the `SHIFT` of the origin and the `TILT` of `G68.2`, the `ROTATE` of `G68`, the `MIRROR` of `G51.1`) and writes a change of the control's transforms (`G52` replacing or cancelling, `G69`, `G50.1`) as one cut of the chain with the entries the control keeps written again after it, and only with a cut that means the same under both readings of `SHIFT=RESET` (wave-1 question #95): `SHIFT=RESET` while the chain holds one shift, `ROTATE=RESET` and `TILT=RESET` at the last entry of their kind, `MIRROR=OFF` for the one mirror at the end of the chain; where no such cut exists the block stays `RAW` with the reason. The origin of a `G68.2` directly after a `G52` shift is added to it in one `SHIFT` (`SHIFT X=5`, then `SHIFT=RESET`, `SHIFT X=15`, `TILT ... MOVE=TURN`), and the `G69` writes the `G52` shift again (`SHIFT=RESET`, `SHIFT X=5`); two shifts of one frame are one shift (representation, decided). The blocks of a chain change stand before the main block, each in a block of its own, and after it where the block selects the datum (`FanucFrames.Write`).
  - Subprograms (virtual machine 3.9): a subprogram section starts with the state of its callers. The reader records the state at every call it reads (`FanucCallerState`: the modal groups, the active cycle, the modal F, the chain, the spindle of a bare `S` with `G96` and the speeds, the preloaded tool, polar interpolation, TCPM) and restores, fact by fact, what every call of the file agrees on (`FanucCalls`); a fact the calls do not agree on, or that a call not read yet leaves open (a subprogram in front of its caller, one no block of the file calls), is unknown (`FanucUnknowns`), and a block that depends on it stays `RAW` with the reason: the code of group 01, `G90`/`G91` for an absolute address, `G15`/`G16`, the plane and polar interpolation of an arc or a corner, TCPM for `I J K` on a line, the active cycle, `G98`/`G99` and the plane of a new cycle, the feed mode or the speed of a pitch, the spindle of a bare `S`, `G96`/`G97`, the chain a `G52`, `G69` or `G50.1` acts on; a bare `M6` is a bare `TOOL`. A contour section of `G70`..`G76` takes the state at its cycle the same way; a `CALL` with `TIMES` enters its later passes with what the subprogram may change unknown. The caller continues with the state its subprogram left: after a call of a subprogram of the file, what the subprogram may change (`FanucChanges`, from its codes and words and those of the subprograms it calls) is unknown until the source sets it, the modal words of those groups are written again, and the positions are forgotten after every call, an external one included (virtual machine 3.9). A program starts from the initial state of the control that its header writes (D34), no longer from the end of the program before it in the file.
  - Nakamura `M200` (controller-mapping 6, `CALL` and `TIMES`; controller-mapping 8): on a machine of the builder nakamura `M200 P{times}{nnnn}` reads as the older `M98` form, `CALL` with `TIMES` from the leading digits, and the called `O` program of the file is a `SUB`; `FanucMacro.CallsOf` gives the calls to the structure pass and to the reader alike. The WARNINGs `RDR100` (`MFUNC`) and `RDR105` are reported only for a block that is written, not for one that stays `RAW` (`FanucDraft.Warnings`); `FanucMacro.CalledProgram` no longer throws on a `P` beyond a long (code-guidelines 4).
  - New behaviour questions, `TODO(question)` in the code with `RAW` as the least committal reading: a `G52` while the chain holds a `G68`, `G68.2` or `G51.1` (`FanucFrames.ReadShift`); a second `G68` or `G68.2` before the `G69` (`FanucFrames.ReadRotation`, `FanucTilt.ReadTilt`); how a reader reads a subprogram whose blocks depend on callers in different states or on a call it has not read (`FanucReader.BeginSection`). Wave-1 question #95 is cited where the cut of the chain depends on it.
  - Shared file: `src/Ncx.Readers/SourceState.cs` gets `internal void SetModalGroups(IReadOnlyDictionary<int, string>)`, so that a reader starts a section from the modal state it knows there (a program with none set, a subprogram with its callers'); one test in `tests/Ncx.Readers.Tests/SourceStateTests.cs`.
  - Tests: `G69_AfterAG682WithAnOriginOverAG52_KeepsTheG52Shift`, `G52_WhileAG682IsActive_IsKeptAsRaw`, `G52_ReplacedWhileAG68Rotates_IsKeptAsRawAndTheRotationStays`, `G682AndG68_WhileOneIsActive_AreKeptAsRaw`, `M200_OfTheBuilderNakamura_IsACallWithTimes`, `M200_OnAMachineOfAnotherBuilder_IsKeptAsRawWithoutAnMfuncWarning`, the new `FanucSubprogramTests` (a subprogram under `G81` with `CYCLE_CALL` per position that checks without ERROR, one after `G0` moving at `G0`, callers in different states of group 01 and of `G90`/`G91`, a block after the call of a subprogram that sets group 01, the chain of a subprogram no block calls and of one whose caller rotates, a second program from the initial state), `SetModalGroups_OfASectionStart_ReplacesEveryActiveCode`.
  - Done when: unchanged, (1) holds for the reader half with its question, (2) holds (`Expected/BOHREN.ncx` unchanged), (3) the test exists and skips without `NCX_CORPUS`. Remaining for part two: `ncx convert` in `Ncx.Cli` (D77, with P1-07 and P2-04 part two). Open work left in the reader: `G16` under `G91`.
- Claude (agent), 2026-09-13, part two, the command (P3-02c, branch `p3-02c-convert`): `ncx convert <file> --machine <toml> [--output <file>] [--strict]` in `src/Ncx.Cli/Commands/ConvertCommand.cs`, for the Fanuc and the Heidenhain reader. Implements D5, D77, D91, D97, D98, D103; the reader rules and program rewriters of the plugins (D106) join with P7-01, a plain `TODO` in the code.
  - The run: the controller program and the machine are read first (`InputFile`, `RunMachine.Select` of P2-04: `--machine` by name or path, else the machine of `ncx.toml`); the controller of the machine chooses the reader from the registry that `Program.Readers()` fills, one line per family (`Controller.Fanuc` to `FanucReader`, `Controller.Heidenhain` to `HeidenhainReader`); the reader reads, `NcxWriter.Write` writes; then the expander and the virtual machine STATIC over the program the reader produced, whose diagnostics follow the reader's (architecture 7, 10). The text goes to the standard output or into the file of `--output`, the diagnostics to the standard error.
  - Codes `CLI250` (`MachineRequired`: neither `--machine` nor `ncx.toml` names a machine file, exit 2 before the run, D77, D97, D103) and `CLI251` (`NoReaderForController`: the machine's controller has no reader, today Siemens until P5-01; an ERROR on the program, exit 1, since the inputs were read and D97 gives exit 2 only to a usage error, an unreadable input and a missing machine file) in `src/Ncx.Cli/DiagnosticCodes.Convert.cs`; the range is `CLI250`-`CLI299`.
  - Readings decided while doing it: `--machine` is "required" as architecture 10 says it, a machine file from `--machine` or from `ncx.toml`, never the built-in default machine (`RunMachine.IsDefault`); `CLI250` is reported as a mistake of the command line, `ncx(1)`, the form of a usage error (wave-1 question #79). The STATIC check runs over the program the reader produced, expanded as `ncx check` expands, not over a parse of the written text: the flowchart of architecture 10 has no parser in convert, and the blocks carry the lines of their source blocks (architecture 7, `Begin(sourceLine)`), so what the check finds cites the controller program on the line of the source block, as the reader's diagnostics do (`spindle.nc(6): WARNING VM500` for the `G1` of line 6). An ERROR of the reader stops the check before its first block (virtual machine 2.9, as for a parser ERROR). The check reports each `RAW` block it runs as well (`VM400`, virtual machine 5), next to the reader's `RDR001` (D5): `2.5D_FRAESEN.h` gives `RDR010`, `RDR001` twice, `VM400` twice. A byte order mark is no part of the program the reader reads (wave-1 question #80). The text ends its lines with LF, the writer's rule for a program a reader built.
  - Code structure (decided, nothing a user sees): the writing of `--output` moved from `FormatCommand` into `src/Ncx.Cli/OutputFile.cs` on its second use, as P1-07 moved `InputFile`; `RunMachine` got the property `IsDefault`; the convert stages stay in the command (as `FormatCommand` calls the parser and the writer), since `Pipeline.Run` reads an NCX file from disk and only the three lines of expand and run would be shared.
  - Behaviour questions, `TODO(question)` in `ConvertCommand.Run` with the least committal reading: the encoding of a controller program (read as UTF-8 like every input, a program whose bytes are no UTF-8 is `CLI002`, exit 2); where the text goes, architecture 10 giving convert "`.ncx` in the working directory" without naming the file (the standard output or `--output`, as `ncx format`; nothing is written into the working directory that the command line does not name); whether the text is written when the reader or the check reports an ERROR (it is written, the reader always producing a whole program with `RAW` for what it cannot read, and the ERROR sets exit code 1).
  - Shared files: `src/Ncx.Cli/Program.cs` (the command, `Readers()`, `ToolName` and `CommandLine` internal), `RunMachine.cs` (`IsDefault`), `RunSettings.cs` (one sentence of the summary), `Commands/FormatCommand.cs` (`OutputFile.Write`), `DiagnosticCodes.cs` (the range line); the READMEs of `src/Ncx.Cli/`, its `Commands/`, `tests/Ncx.Acceptance/Cli/` and `Examples/`; `tests/Ncx.Acceptance/Examples/FanucReaderTests.cs` and `HeidenhainReaderTests.cs`, whose lists of the blocks the example reads otherwise became `internal`, so that the command is compared with the same lists.
  - Not edited, for the merge step: the row of convert in architecture 10 (F28) does not name `--output`, `--strict` or the machine of `ncx.toml`, and gives the output as "`.ncx` in the working directory" (the question above); the task file of P3-05, whose criterion (1) now holds through the command (`ConvertExampleTests.CommandLine_25DFraesenHeidenhain_EqualsTheBlocksOfTheExampleWithTheWarningAboutTheMissingM30`).
  - Tests: `tests/Ncx.Acceptance/Cli/ConvertCommandTests.cs` (14 tests through `ConvertCommand.Run` with a working directory of their own and the repository as the tool folder: no machine is `CLI250` exit 2; the machine of `ncx.toml`; Siemens is `CLI251` exit 1; a missing program and an unknown machine both reported, exit 2; a program that is no UTF-8; a machine file with an ERROR; `--output` and an output file that cannot be written; a reader ERROR writes the text and stops the check; the check cites the source line; the check's diagnostics after the reader's; `--strict`; a byte order mark; `convert` without its file is a usage error) and `tests/Ncx.Acceptance/Examples/ConvertExampleTests.cs` (through `Program.Run` with the machines by name, as `dotnet run` runs them: 2.5D_FRAESEN from the Fanuc source equals the example blocks with the ten blocks of `FanucReaderTests`, without a diagnostic; from the Klartext source with the two blocks of `HeidenhainReaderTests` and `RDR010` about the missing `M30`; `BOHREN.fanuc.nc` equals `Expected/BOHREN.ncx` byte for byte; the eight example sources give what their reader reads through the library, without an ERROR; the corpus through the command, skipping without `NCX_CORPUS`). A mutation check broke four rules in turn (the default machine accepted, the check's diagnostics dropped, `--output` ignored, nothing written on ERROR); each failed one to three of the tests.
  - The acceptance run from the repository root: `dotnet run --project src/Ncx.Cli -- convert docs/spec/examples/sources/2.5D_FRAESEN.fanuc.nc --machine fanuc-mill-30i` exits 0 without a diagnostic, and its blocks equal those of `docs/spec/examples/2.5D_FRAESEN.ncx` with comments and trivia stripped except the ten blocks the example reads otherwise for Fanuc; `... convert docs/spec/examples/sources/2.5D_FRAESEN.h --machine heidenhain-itnc530` exits 0 with the WARNING about the missing `M30`, equal except `NUMBER=1` and the two `BLK FORM` blocks kept as `RAW`.
  - Done when: (1) holds, through the command, with the question of part one about the ten blocks (`FanucReaderTests`); (2) holds, through the library and the command; (3) holds as far as the repository can hold it: the corpus test exists for the reader and for the command and runs when the maintainer sets `NCX_CORPUS`. The task moves to `done/`. Open work left in the reader: `G16` under `G91` (a `TODO` in the code).
