# P3-04 Heidenhain compiler

Phase: 3 | Milestone: M5 | Depends on: `P3-03`, `P3-02` | Size: L (S: a day or two, M: up to a week, L: more)

## Goal

NCX to iTNC 530 Klartext, per section 8 of `controllers/heidenhain.md`.

## Scope

- `BEGIN PGM`/`END PGM` with consecutive block numbers, the comma, signs on coordinates, `L`/`FMAX`/`F`, `CC` plus `C` and `CR`, `CP IPA` for sweeps, `TOOL CALL` assembled from `TOOL`, `WORKPLANE` and `RPM`, `TOOL DEF` for `PRELOAD`, `RL`/`RR`/`R0`, `CYCL DEF 247`, `CYCL DEF 7`, `CYCL DEF 8`, `CYCL DEF 10`, `PLANE SPATIAL`/`AXIAL` with the options from `MOVE` and `ROT`, `M91` moves for `HOME`, `M128`/`M129` or `FUNCTION TCPM`, `M126`/`M127`, `M116`/`M117`, `M140 MB MAX`, `CYCL DEF 32`, cycle blocks with `Q` parameters and `CYCL CALL`/`M99`, `LBL` subprograms after `M30` per calling program, `CALL LBL REP`, `FN 9`..`FN 12` and formulas for `JUMP`/`IF`/`VAR`.
- `ncx compile` wired to the compiler.

## References

- controllers/heidenhain.md section 8
- controller-mapping.md (Heidenhain column)
- examples/sources/2.5D_FRAESEN.h
- `BOHREN.ncx` as read from `BOHREN.fanuc.nc` by the Fanuc reader (P3-02)

## Done when

- `ncx compile examples/2.5D_FRAESEN.ncx --machine heidenhain-itnc530` is equivalent to `2.5D_FRAESEN.h` under the comparison rules (block numbers, formatting and comment placement ignored) (M5).
- `BOHREN.ncx` (from the Fanuc source, read by P3-02) compiles to a program equivalent to `BOHREN.h`.

## Log

(who, when, what was decided while doing it; a decision that changes the specification gets a row in `../../../decisions/decisions.md`)

- Claude (agent), 2026-09-14: section P3-04 of `13-phase-3-readers-compilers.md` in full, in `src/Ncx.Compilers/Heidenhain/` (namespace `Ncx.Compilers.Heidenhain`) with the codes `CMP100`-`CMP112` in `src/Ncx.Compilers/DiagnosticCodes.Heidenhain.cs`, the unit tests in `tests/Ncx.Compilers.Tests/Heidenhain/` and the acceptance in `tests/Ncx.Acceptance/Examples/HeidenhainCompilerTests.cs`; `ncx compile` chooses the compiler through one registration line in `Program.Compilers()`. Implements heidenhain 8 rules 1 to 7 and D31, D34, D48, D49, D82, D83, D84, D85, D86, D91, D94, D99, D100.
  - Files: the phase plan's `HeidenhainCompiler`, `HeidenhainProgramFrame` (`BEGIN PGM name MM`, `M30`, `END PGM` after the subprograms, `LBL 0`, `;` and `* -`, `RAW`), `HeidenhainMotion` (`L`/`LN`, `FMAX`, `F` on change, `R0`/`RL`/`RR`, `M91`, `IX`), `HeidenhainToolCall`, `HeidenhainFrames` (`HOME` as `M91` moves, `M140 MB`, `M128`/`M129` or the `[transform]` templates for `FUNCTION TCPM`, `M126`/`M127`, `M116`/`M117`, cycle 32), `HeidenhainCycles`, `HeidenhainSubprograms`, `HeidenhainFlow`, `HeidenhainNumbers`; and, to keep each file one screen (code-guidelines 10.2), `HeidenhainArcs` (`CR`, `CC` plus `C`, `CC` plus `CP IPA`), `HeidenhainChain` (`ORIGIN` as cycle 247, the chain as cycles 7, 8, 10 and `PLANE` through `ChainWriter`, D31), `HeidenhainCycleValues` (the `Q` values), `HeidenhainFormula` (the `Q` formulas and the operands of `FN 9`..`FN 12`), `HeidenhainFunctions` (`M136`/`M137`, spindle, coolant, `[func]`, `MFUNC`, `M0`/`M1`), `HeidenhainAxes` with `HeidenhainAxisWord`, and `HeidenhainBlock`, the block being written, which hands the concerns the protected members of `CompilerBase` as delegates.
  - Decided while doing it (representation and derivations from the documents, no question): a block is written concern by concern in the order the control executes it: structure, comments and `LBL`, then every state word in a Klartext block of its own before the motion (language 5 rule 3), the motion, then `DWELL`, `STOP`, the calls and the jumps, the end of the section; a word no concern writes is the ERROR `CMP101`, so nothing is dropped silently (language 2 rule 8). `R0`/`RL`/`RR` and `F` are written through the target state where they change, so the `COMP` and `F` of a block without a motion stand on the next line (as the header's `COMP=OFF` becomes the `R0` of the first positioning of both sources). `CC` is written before every `C` and `CP`; an `ARC` with `ANGLE` is always `CC` plus `CP IPA`, the sweep form of virtual machine 3.2; a full circle is `C` to its start. The `RPM` fold also takes the `WORKPLANE` of the following block. The `LBL` of `JUMP=END` is one more than every numbered `LABEL` and `SUB` of the file and stands before the `M30` (controller-mapping 1, `JUMP=END`); a `JUMP` without `IF` is `FN 9: IF +0 EQU +0`. `CALL` with `TIMES=n` is the `CALL LBL` line n times (virtual machine 3.9). The block number of the source goes from a `RAW` text (heidenhain 8 rule 1). `Q` values carry their sign as heidenhain 5 writes them (`Q200=+2`). `ORIGIN` cancels the entries it removes before cycle 247 (holds whatever D253 answers); a transform appended while one of its kind stands in the chain is `CMP106` (D253). A word of a cycle block that the entry maps to no `Q` parameter is the WARNING `CMP105` (`CYCLE_F` and `CYCLE_DWELL` of `TAP`).
  - Recommendations followed where the question is on record: D163 (`Q211` = `CYCLE_DWELL` or 0; `Q213` = 0 for `PECK`, the number of infeeds for `CHIP_BREAK`), D164 (`Q202` of 200 the depth, `Q205` = `Q202`, `Q208` = `MAX` on 203 and `Q206` on 201, `Q210`, `Q212` 0, `Q256` 0.6), D226 (`Q204` = `Q200` for `CYCLE_RETRACT=CLEARANCE`), D178 (an entry without signature is the ERROR `CMP103`), D212 (`REP 1` for a `REPEAT` without `TIMES`), D185 (`RAPID` with a tool vector as `LN ... FMAX`), D252 (every function in a block of its own before the motion), D151 and D180 (the machine's templates as they are); each marked `TODO(question)` with its number.
  - Behaviour questions, `TODO(question)` in the code with the least committal reading: (1) the target state at the start of a program: the header's `FEED_MODE=PER_MIN` is `M137`, which the Klartext sources do not write (`HeidenhainFunctions.WriteFeedMode`); (2) "the following block" of rule 3 across the `PRELOAD` that `TOOL DEF` writes after the call (`HeidenhainToolCall.FollowingBlock`; `Expected/BOHREN.ncx` has `TOOL=1`, `PRELOAD=2`, `SPINDLE=CW RPM=10000`); (3) the name after `CYCL DEF n` (`BOHREN`, `UNIVERSALBOHREN`), written as the documents' examples give it, the number alone otherwise (`HeidenhainCycles`); (4) a helix of at most one turn with an end point, `C` with the tool-axis word or `CP IPA` (`HeidenhainArcs.WriteCenter`); (5) `F` "on every block by option" without a key in machine-config 2 (`HeidenhainMotion.Feed`); (6) subprograms "as separate .h files with CALL PGM when the machine setting says so" without such a setting (`HeidenhainSubprograms`); (7) `RETURN`, whose `sub_end` `LBL 0` closes the section where it stands (`HeidenhainFlow.WriteReturn`, `CMP101`); (8) the words of `FN 10` and `FN 11`, written `NE` and `GT` (`HeidenhainFlow.Condition`); (9) a `WORKPLANE` without `TOOL` that changes the axis of the last `TOOL CALL` (`HeidenhainToolCall.CheckWorkplane`, `CMP110`); (10) a formula in a coordinate (`HeidenhainNumbers`, `CMP102`); (11) `LBL "NAME"` on the iTNC 530 (`HeidenhainFlow.Label`); (12) `ROT` of a `PLANE`, for which `[transform]` has no placeholder (`HeidenhainChain.WriteTilt`, WARNING `CMP108`); (13) `FUNC:RIGID_TAP=ON` of `Expected/BOHREN.ncx` (the Fanuc `M29 S500`), which `heidenhain-itnc530.toml` does not name, the ERROR `VM006`, so the acceptance takes its block out before compiling (`HeidenhainCompilerTests`).
  - The comparison rules of phase 3 are `tests/Ncx.Acceptance/Examples/NcEquivalence.cs` with `NcException`, for P3-07 to adopt into its `NcComparer`: block numbers, `;` and `( )` comments, the `* -` structuring blocks and blank lines removed, the `~` lines of a block joined, runs of blanks made one, every number formatted with the decimals of its address and the separator of the machine without a plus sign; the first block and every `TOOL CALL` or `T M6` block compared as a set of words; every exception grounded in its question, used exactly once. Wave-1 question #12 (answered by D49): its disposition asks for a sentence in the comparison rules of the phase file ("the program_end line is not a difference where the source ends in another form or in none"); the phase files are the bookkeeping's, so the acceptance carries it as an `NcException` and the sentence is left to BK-01.
  - Open work, a plain `TODO` in the code: `SETPOS`, which machine-config 3 folds into `SHIFT` on Heidenhain, is `CMP101` (done by the review fix below); a `SKIP` block with `TOOL` or `PRELOAD` is `CMP111`, since the tool change of the framework writes its lines without the `/`.
  - Tests: 84 in `tests/Ncx.Compilers.Tests/Heidenhain/`, one or more per rule of heidenhain 8 (`Rule1_` to `Rule7_`) and for the frames, the modes, the flow and the functions; 7 of the comparison in `NcEquivalenceTests`; the acceptance in `HeidenhainCompilerTests`, which also compiles `PATTERN_LOOP.ncx` and `INCREMENTAL_SUB.ncx` without an ERROR.
  - Shared files: `src/Ncx.Cli/Program.cs` (the registration line and its `using`), `tests/Ncx.Acceptance/Examples/README.md` and `tests/Ncx.Compilers.Tests/README.md` (one sentence each).
  - Done when: (1) holds: `dotnet run --project src/Ncx.Cli -- compile docs/spec/examples/2.5D_FRAESEN.ncx --machine heidenhain-itnc530` writes `out/heidenhain-itnc530/2.5D FRAESEN.h`, equivalent to `sources/2.5D_FRAESEN.h` except the header's `M137` (question 1), the two `BLK FORM` blocks the example holds as a comment (D217, heidenhain 7 rule 9) and the `M30` the source leaves out (wave-1 question #12); `Compile_25DFraesen_IsEquivalentToTheHeidenhainSourceExceptTheListedDifferences`. (2) holds: `Expected/BOHREN.ncx` compiles to a program equivalent to `BOHREN.h` except `M137`, `BLK FORM`, the plunge feed `Q206=565` of the Fanuc source against `565,487`, `Q213=19` against 24 (D163), the fourth operation, single moves in the Fanuc source against cycle 203 with `Q213=3` (D163), and `M30`, with the `FUNC:RIGID_TAP=ON` block taken out first (question 13); `Compile_Bohren_IsEquivalentToTheHeidenhainSourceExceptTheListedDifferences`. The task file moves to `done/`.
- Claude (agent), 2026-09-14, review fix (the blocking finding: `SETPOS` was `CMP101` against the sentence of machine-config 3, "Heidenhain: none, the compiler folds it into SHIFT"). The branch was rebased onto main first; the one conflict, the sentence of `tests/Ncx.Compilers.Tests/README.md`, keeps main's text with the Heidenhain sentence after it.
  - `SETPOS` is folded into the cycle 7 datum shift in the new `HeidenhainSetpos.cs`, called from `HeidenhainChain.Write` after the entries its block appends (virtual machine 3 step 4): cycle 7 with the setpos shift the virtual machine records, newSetposShift = oldPos - declared (virtual machine 3.4, D55, D101), for every axis the block names and every other axis whose setpos shift stands, since a new cycle 7 replaces the previous one (heidenhain 3). `RAPID X=10 Y=10 Z=5`, `SETPOS X=0` is `CYCL DEF 7.0 NULLPUNKT`, `CYCL DEF 7.1 X+10`. `ORIGIN` cancels it with cycle 7 at 0 before cycle 247, where it stands in the unwinding (D31, the D253 workaround of the chain).
  - Decided while doing it (representation and derivations from the documents, no question): the target state keeps the place of the cycle 7 of `SETPOS`, the number of chain entries it stands after; it is unknown at the start of a program and of a subprogram walk, where the checks below take the conservative side. A setpos shift of 0 counts as no cycle 7, as a `SHIFT` cancelled with 0 does in `Append`. A `ROTATE`, `MIRROR` or `TILT` appended after a `SETPOS` stands after its cycle 7 (language 4.2: applied to the frame active where it stands), and its `RESET` leaves the cycle 7 as it is; a `RESET` of an entry before the cycle 7 that turns none of the axes with a setpos shift is written as well (the two act the same in either order).
  - Recommendations followed: D253 (`CMP106` for a `SETPOS` while a `SHIFT` stands, a `SHIFT` while a setpos shift stands, and a `SETPOS` whose cycle 7 would replace an earlier one that transforms of the chain follow); D117 (an incremental word under `SETPOS` is left unwritten, the ERROR `CMP101`).
  - Behaviour questions, `TODO(question)` in the code with the least committal reading: (14) `SETPOS` of an axis known in the MACHINE frame only, after `HOME` or a machine-frame move: the virtual machine records the setpos shift against the machine position (D101), and without a datum table (D35) the compiler does not know the shift against the active preset that cycle 7 takes, the ERROR `CMP113` (`HeidenhainSetpos.NamedAxes`); (15) a `RESET` that removes a `ROTATE`, `MIRROR`, `TILT` or `TILT_AXIS` that stood before the cycle 7 of a `SETPOS` while the setpos shift of an axis it turns stands: language 4.2 applies `SETPOS` where it stands and lets a `RESET` remove chain entries only, virtual machine 3.4 reads the setpos shift on top of the chain, and the Klartext cancel of a transform the cycle 7 follows is D253, the ERROR `CMP114` (`HeidenhainSetpos.CheckRemoved`). A `SETPOS` of an expression is `CMP102`, the question (10) of a formula in a coordinate.
  - Codes: `CMP113`, `CMP114`. Tests: 12 in `HeidenhainSetposTests` (`Setpos_OfAPositionInTheWorkpieceFrame_IsFoldedIntoCycle7` and the cases above); `HeidenhainFunctionTests.WordWithoutKlartext_IsTheErrorCmp101` takes `POLAR=ON` as its example instead of `SETPOS`; 96 tests in `tests/Ncx.Compilers.Tests/Heidenhain/`. No shared file changed.
  - Done when: (1) and (2) hold as before (neither example has `SETPOS`); the task file stays in `done/`.
- Claude (agent), 2026-09-14, second review fix (the two blocking findings: `RETURN` was `CMP101` against the sentence of machine-config 2, "written for SUB=END and RETURN"; a `UNITS` in a subprogram was dropped without a diagnostic, and `END PGM` took the units of the state at its end).
  - `RETURN`: in a subprogram it is the `sub_end` of the machine, `LBL 0`, where it stands (`HeidenhainFlow.ReturnLabel`, through the new `HeidenhainProgramFrame.WriteSubEnd`, which `SUB=END` uses as well). In a program it is treated as `JUMP=END` (language 4.9, virtual machine 3.6): `FN 9: IF +0 EQU +0 GOTO LBL n` to the `LBL` before the `M30`, and `JumpsToTheEnd` counts a `RETURN` of the program. The parser already refuses an `IF` without `JUMP` or `CALL` (`PAR016`, language 5 rule 5), so a `RETURN` has a condition only in a block with one of them. A `JUMP` in the same block (`RETURN` plus `JUMP`, D221) is written after the return, where neither the virtual machine nor the control reaches it. Question (7) of the first entry is replaced by (16) below.
  - `UNITS`: `TakeUnits` compares a `UNITS` in the walk of a subprogram with the units of the `BEGIN PGM` of the program whose file the walk is written into, which is the last `PROGRAM=BEGIN` before it in walk order (`HeidenhainProgramFrame.ProgramOf`). `END PGM` takes the units of `BEGIN PGM` (the header state), not those of the state at `PROGRAM=END`. The reviewer's reproducer is now `CMP101` on the `UNITS=INCH` of the subprogram, with exit 1.
  - Decided while doing it (derived from the documents, no question): the walk of a subprogram that no program calls follows the last `PROGRAM=END` and belongs to no program. Under `file_per_program` it stands in no file (the framework's WARNING and its `TODO(question)`), so its `UNITS` is compared with nothing.
  - Behaviour questions, `TODO(question)` in the code: (16) heidenhain 7 rule 3 reads "`LBL n` closed by `LBL 0`" as a subprogram, and no document says how a reader takes an `LBL 0` that stands before the one closing the section, which `sub_end` for `RETURN` writes (`HeidenhainFlow.ReturnLabel`). On record, followed as the least committal reading: `RETURN JUMP` under `IF` in a subprogram (D221). `IF` makes the whole block conditional (language 4.9), which `LBL 0` cannot express, so it is `CMP101`, as a `CALL` under `IF` is.
  - Tests: `Return_InASubprogram_IsTheSubEndLbl0WhereItStands`, `Return_InAProgram_IsTheJumpToTheLblBeforeTheM30` and `Return_UnderIfInASubprogram_IsTheErrorCmp101` replace `Return_BeforeTheEndOfASubprogram_IsTheErrorCmp101` in `HeidenhainFlowTests`. `HeidenhainProgramFrameTests` gains `Units_ThatChangeTheUnitsOfBeginPgmInASubprogram_IsTheErrorCmp101` (the reproducer) and `Units_OfASubprogramThatKeepTheUnitsOfBeginPgm_WriteNothing`. There are now 100 tests in `tests/Ncx.Compilers.Tests/Heidenhain/`. No shared file changed.
  - Done when: (1) and (2) hold as before (neither example has `RETURN` or a `UNITS` in a subprogram); the task file stays in `done/`.
- Claude (agent), 2026-09-18, third review fix (the three blocking findings: a `COMP` of a block without a motion was written only on a later `L` line, so `HOME`, an arc, a bare `CYCL CALL` and `M140` ran with the old compensation; the full circle after a `SETPOS` ended at the physical position instead of the workpiece coordinate; labels of one output file could be written as the same `LBL`, or as `LBL 0`). The branch was rebased onto main first; the one conflict, the sentence of `tests/Ncx.Acceptance/Examples/README.md`, keeps the Heidenhain sentence with main's Siemens sentence after it.
  - Compensation: `HOME` is an `L` block and carries the `R0`, `RL` or `RR` of the state after it where the control has another one (`HeidenhainFrames.WriteHome` through `HeidenhainMotion.CompensationWord`, language 4.4, heidenhain 2): the reviewer's `COMP=OFF`, `HOME Z` now gives `L Z+0 R0 FMAX M91`. An arc, a `CYCL CALL` at the current position and `M140` carry none in the documents (heidenhain 2 and differences.md write them at the end of the `L` block), so `HeidenhainMotion.CheckCompensation` reports the new ERROR `CMP115` where the control would run them with another compensation than the program has after the block. What the control has is the compensation written last or, where the target state does not know it (the start of a program or of a subprogram walk, D99), the one of the program before the change, which `KeepPending` now records when a block without an `L` changes it; so an arc in a subprogram entered with `RL` after its own `COMP=OFF` is reported too. After the ERROR the target takes the program's compensation, so that one change is reported once. A `COMP` that no motion follows before `PROGRAM=END` is not reported: it moves nothing, and `PROGRAM=END`, the `M30` of `program_end`, resets the compensation (virtual machine 4, "feed value and mode, compensation ... reset by PROGRAM=END"). The misleading comment on `WriteArc` is gone.
  - Changed expected output: `HOME` as the first motion of a program now writes `R0`, as the first `L` does, since the control's compensation is not known yet (`Rule5_Home_IsAnM91MoveToTheReferenceCoordinates`: `L Z+0 R0 FMAX M91`; `Rule5_HomeOfPoint2_IsTheM91MoveToHome2`: `L Z-100 R0 FMAX M91`). Neither example changes: `ncx compile docs/spec/examples/2.5D_FRAESEN.ncx --machine heidenhain-itnc530` writes the same file as before, and its `HOME` lines follow an `L ... R0`.
  - Full circle: `C` ends at the start in the workpiece frame, the stored position less the setpos shift of the axis, the stored value where that shift is unknown (virtual machine 3.4, D101), through the new `HeidenhainSetpos.WorkpieceCoordinate`; a start known in the MACHINE frame only is reported as an unknown start (`CMP101`), as an unknown one was.
  - Labels: the new `HeidenhainLabels.Check`, called with `BEGIN PGM`, walks the steps of the program, the walks of the subprograms it calls included (each block once), and reports the new ERROR `CMP116` for a `LABEL` or `SUB` name that would be written as an `LBL` another one of the program already has (language 4.9 keeps `LABEL` unique per section and the `SUB` names apart; heidenhain 1 and language 4.13: the `LBL` sections stand in the program, labels are program-local), and for one written as `LBL 0`, which ends a subprogram. The `LBL` of `JUMP=END` is one more than every number and cannot collide.
  - Behaviour questions, `TODO(question)` in the code with the least committal reading: (17) whether a `C`, `CR` or `CP` block, a `CYCL CALL` or `M140` may carry `R0`, `RL` or `RR` (`HeidenhainMotion.CheckCompensation`, the ERROR `CMP115`); (18) whether the compiler may renumber labels that collide in one program file, and what `LABEL=0` becomes (`HeidenhainLabels.Check`, the ERROR `CMP116`, nothing renumbered).
  - Codes: `CMP115`, `CMP116`. Tests (110 in `tests/Ncx.Compilers.Tests/Heidenhain/`): `Rule5_HomeAfterCompOff_WritesR0OnTheM91Line`, `Retract_AfterACompensationChangeNoLineWrote_IsTheErrorCmp115`, `Rule2_CompensationChangeThatNoMotionFollows_IsResetByTheM30`, `Rule4_FullCircleAfterSetpos_EndsAtItsStartInTheShiftedFrame`, `Rule4_ArcAfterACompensationChangeNoLineWrote_IsTheErrorCmp115`, `Rule4_ArcAfterCompOffInASubprogramEnteredWithRl_IsTheErrorCmp115`, `Rule7_CyclCallAfterACompensationChangeNoLineWrote_IsTheErrorCmp115`, `Labels_LabelAndSubprogramOfOneNumberInOneFile_AreTheErrorCmp116`, `Labels_OneLabelInTheProgramAndInItsSubprogram_AreTheErrorCmp116`, `Labels_LabelZero_IsTheErrorCmp116`. No shared file changed; the folder README of the compiler and of its tests name `HeidenhainLabels`.
  - Done when: (1) and (2) hold as before (`HeidenhainCompilerTests`; neither example has a `COMP` without a motion before `HOME`, an arc, a `SETPOS` or colliding labels); the task file stays in `done/`.
