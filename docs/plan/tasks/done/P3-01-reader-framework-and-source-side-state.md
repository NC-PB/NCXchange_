# P3-01 Reader framework and source-side state

Phase: 3 | Milestone: M4 | Depends on: `P1-07`, `P2-04` | Size: M (S: a day or two, M: up to a week, L: more)

## Goal

What every reader shares: tokenizing a controller file, keeping the source controller's modal state, emitting NCX through the builder, keeping what it cannot read as `RAW`.

## Scope

- `IReader` (`Read(SourceFile, MachineConfig) -> NcxProgram`), a `SourceBlock` tokenizer per family, a `SourceState` (the source-side small virtual machine: modal groups, positions, active tool and offsets, active cycle, master spindle) that the mapping rules consult.
- The `RAW:CONTROLLER` and `RAW:BUILDER` fallback with a WARNING per block; nothing is dropped (D5).
- The `ISourceRule` hook (plugins, phase 7) as a public interface in `Ncx.Readers`, where its caller is (D106): a rule sees the source blocks and the source-side state and may claim a sequence (D66).
- Comment and section words, block skip, the file structure (programs and subprograms as sections, jump-entered code moved in front of the end behind `JUMP=END`, D48, D89).

## References

- architecture.md section 7 (readers, with the sequence diagram)
- ncx-language.md section 4.13
- controller-mapping.md sections 1 and 6

## Done when

- A fake reader with three rules reads a three-line input into the expected NCX text.
- `RAW` blocks survive a `format` round trip.

## Log

(who, when, what was decided while doing it; a decision that changes the specification gets a row in `../../../decisions/decisions.md`)

- Claude (agent), 2026-09-13: section P3-01 of `13-phase-3-readers-compilers.md` in full, in `src/Ncx.Readers/` (namespace `Ncx.Readers`) with the tests in `tests/Ncx.Readers.Tests/`. Implements D5, D40, D48, D66, D89, D92, D98, D105, D106.
  - Types. Public, because the CLI registers the readers and plugins write rules: `IReader` (`Controller`, `Read(SourceFile, MachineConfig, ReadOptions)`), `ReaderBase`, `ReaderRegistry` (keyed by the `Controller` of the machine file, a second registration throws), `ReadOptions` (`Rules`), `SourceFile` (`Name`, `Text`), `ISourceTokenizer`, `SourceBlock` (`Line`, `Text`, `Words`, `Comment` without the controller's delimiters, `BlockSkip`, `SkipSwitch` for `/n`, `Continuation` as the texts of the following lines), `SourceWord` (`Address`, `Text` as written after the address, `Number`, `Expression`, `ToNcxNumber()`: `70.` is 70, `-.534` is -0.534, `+10` is 10, `-6,964` is -6.964, the digits otherwise as written), `SourceState`, `ISourceRule`, `SourceStructure` and `StructureRole`. Internal: `StructurePass`, `ReadStep`, `SourceSection`, `RawEmitter`, `NativeCode`. A family reader is public so that `Program.cs` can register it, so `ReaderBase` is public too.
  - `SourceState` holds every fact of architecture 7 with public getters and internal setters: plugins read it, the family readers of this assembly set it. `Apply` (internal, called before a block is read, since the codes of a block act for the block) keeps the active code of every modal group from a table the family gives (`ModalGroupOfCode`, codes without leading zeros); everything else depends on the family's addresses and is set by its `ReadBlock`.
  - Template method: tokenize; `StructureOf` per block (the family says: file frame, program begin, subprogram begin, O-like section begin, program end, return, label, labels used); `StructurePass.Plan`; then per step a trivia line, a structural block, or a read: `State.Apply`, the rules when the family's `LeavesUndecided` says the tables and the controller's own words leave something open (the tables are tried first, D40, machine-config 5), else `ReadBlock`. Helpers for the families: `BeginBlock` (adds `SKIP`/`SKIP=n` and the source comment on the first NCX block of the source block), `EmitRaw`, `EmitSection` (`* -`), `FindFunction` (TemplateSet, compared by number, D105), `IsBuilderCode` (`[raw]`, compared by number).
  - Structure pass: source blocks are read in file order. Code a program keeps after its first unskipped `M30`/`M2` stays where it is; the `M30` writes `JUMP=END` and the `PROGRAM=END` with the `M30`'s line is written after that code, which is the section moved in front of the end of language 4.13 without moving a block. A later end in that code is `JUMP=END` (the `N2 M2` case), a skipped end `SKIP JUMP=END`. Code after the end that no jump enters is kept the same way (language 4.13: nothing follows the end of a program); the VM reports it unreachable. A Fanuc `O` section is a program when it holds `M30`/`M2`, a subprogram otherwise, and when no section ends the program the first `O` section is the program (its `M99` loops). `M99` in a program: `LABEL=START` after `PROGRAM=BEGIN`, `JUMP=START`, `SKIP JUMP=START` for `/M99`. In a subprogram the last unskipped return is `SUB=END`, every other return `RETURN` (language 4.9). `LABEL=n` is written for a label that a block of the same section jumps to. Blocks before the first section begin are a program without a name; the file frame is supplied where the source has no `%`. A program without an end gets `PROGRAM=END` after its last block and `RDR010`, a subprogram without a return `SUB=END` and `RDR011`, so every reader keeps the contract of code-guidelines 4 (a program `check` can run).
  - Trivia placement with the builder as it is (wave-1 question #77): the builder gives a trivia line the line of the block begun last and the writer writes it before the first block of a greater line. With the blocks read in file order only the deferred `PROGRAM=END` has a smaller line than the blocks before it; the structure pass writes it at the section boundary, after the trivia that precede the boundary, so these trivia carry the line of the last block of the moved code and are written after `PROGRAM=END` and before the next section (the blank lines of `INCREMENTAL_SUB` land where the example has them). What the structure pass needs from the builder for anything more (a real reordering, such as Klartext code entered by `FN 9` that stands after an `LBL` subprogram): trivia placed by position among the blocks, not by line number.
  - Comments (controller-mapping 1, D5, D92): a source comment goes on the first NCX block read from its block; a comment-only line and a blank line are trivia (`; text`, or empty); the comment of a block that writes no NCX block (`G90` alone) or that a rule claimed stays as a comment-only line after it; `( name )` after `O` is `NAME` and is not repeated; `* -` is `SECTION` through `EmitSection`.
  - RAW (D5): the source text verbatim (block skip, block number and comment included, so no `SKIP` word is added), one RAW block per line of a continued block because an NCX string holds no line break, one `RDR001` WARNING per source block; `RAW:<builder>` when the block holds a code of `[raw] known` and the machine names a builder, `RAW:<controller>` otherwise.
  - The machine's templates are parsed per read; a template the machine file cannot use is added to the program's diagnostics under the machine's name, since the record keeps neither its file nor its line (wave-1 question #61).
  - Codes: `RDR001` KeptAsRaw, `RDR010` ProgramEndMissing, `RDR011` SubReturnMissing in `DiagnosticCodes.Raw.cs` and `DiagnosticCodes.Structure.cs`; `DiagnosticCodes.cs` reserves RDR001-RDR099 for the framework.
  - Open questions, marked `TODO(question)`: the fold of `M5`, `M51`, `M3 S1500` by a rule against "the tables are tried first" (`ReaderBase.ReadSourceBlock`); a Fanuc file holding only `Onnnn ... M99` reads as a looping program (`StructurePass.DecideKinds`); the missing-end WARNING for Fanuc and Siemens (`StructurePass.PlanProgram`); `NAME="SLOT_ROW"` in `INCREMENTAL_SUB` against `NAME="2.5D FRAESEN"` in `2.5D_FRAESEN` (`StructurePass.ProgramBegin`); how a rule called once per source block with one method claims a sequence of blocks (D66), since a block the tables decide is not offered to the rules and nothing is called after the last block (`ISourceRule.Read`, `ReaderBase.ClaimedByRule`), a claim covering the one block offered until it is answered.
  - Done when: both criteria hold. `ReaderRulesTests.FakeReaderWithThreeRules_ThreeLines_ReadsTheExpectedNcx` (three rules, three lines, the table wins for `M08`); `RawTests.RawBlocks_ReadAndFormatted_SurviveTheRoundTrip`. Also tested: the jump-entered section of `INCREMENTAL_SUB` with the labels intact, the `M99` loop, `/M30`, the `N2 M2` case, sections, missing ends, block skip, comments, the source-side state, source numbers, codes by number, the registry, the RDR codes. No reader of a family and no `ncx convert` exist yet: the Fanuc reader and the command are P3-02, the Heidenhain reader P3-05.
- Claude (agent), 2026-09-13, review fix: the `ISourceRule` documentation no longer tells a rule to hold blocks back between calls and write them when it declines a later block. With the tables tried first and no call after the last block, that protocol wrote the blocks the tables decide before the held ones and dropped a block held at the end of the file, against D5. A claim now covers the one block offered and is complete when `Read` returns; the sequence claim of D66 is the open question above, marked at `ISourceRule.Read` and `ReaderBase.ClaimedByRule`. A second method or a sentinel call at the end of the input would settle it in the code and break "one method per interface" (architecture 9), so neither was added. Test: `ReaderRulesTests.Rules_ClaimBetweenTwoMotions_RuleBlocksStandInPlaceOfTheBlock`. Done when: both criteria still hold; the sequence claim of the scope waits on the answer to that question.
- Claude (agent), 2026-09-13, second review fix, the kind of a Fanuc `O` section: the rule "a program when it holds `M30`/`M2`, a subprogram otherwise" was cited to controller-mapping 1 and 6, which say only that `Onnnn ... M99` after the caller's `M30` is a `SUB` and that several `O` programs are several `PROGRAM`s. It read an `O` subprogram holding an `M30` (virtual machine 3.6: "as a Fanuc `M30` in a subprogram does") as a program whose `M99` loops, and the caller's `CALL` of it was then the ERROR `VM071` (language 4.13), against the reader contract of code-guidelines 4.
  - Now: `SourceStructure.Calls` (public, empty by default) carries the subprograms a block calls, by name, or by number without leading zeros where the source names them by number (`M98 P0100` as `"100"`). An `O` section that a block of the file calls is a `SUB`, also when it holds an `M30`, which is then `JUMP=END`: a `CALL` of a program is an ERROR, programs are entered from the job only (language 4.13, virtual machine 3.6). Every block of the file counts, the section's own included, so a section that calls itself is never a program that a `CALL` enters (a representation choice).
  - Open question, widened from "a file holding only `Onnnn ... M99`" and marked `TODO(question)` at `StructurePass.DecideKinds`: what an `O` section is that no block of the file calls, an `O` program of its own or a subprogram that another file calls. Until it is answered such a section is a program when it holds `M30`/`M2` and a subprogram otherwise, and when no section is a program the first uncalled one is (language 4.13: at least one program). So a subprogram holding an `M30` that stands in its own file reads as a program with its `M99` as the loop. The controller-mapping cites are gone from `DecideKinds` and `StructureRole.SectionBegin`; the `M30`-in-a-subprogram comment of `PlanSub` now names the called `O` section.
  - For P3-02: the Fanuc reader fills `Calls` for the calls of controller-mapping 6 that enter an `O` program by number (`M98 P`, the older `M98 P51002` as `1002`, `G65 P`, Nakamura `M200 P`), or a called `O` section holding `M30` becomes a program again.
  - Tests: `StructurePassTests.Sections_CalledOSectionHoldingM30_IsASubWhoseM30IsJumpEnd` (the reviewer's input; `check` finds no ERROR), `Sections_UncalledOSectionHoldingM30_IsAProgramWhoseM99Loops` (pins the interim reading of the question), `Sections_NoOSectionEndsTheProgram_TheFirstUncalledOneIsTheProgram`. `FakeRead.Check` runs the written text STATIC on the default machine of D103, since the fake machine lists no `[[axis]]`. Done when: both criteria still hold.
- Claude (agent), 2026-09-14, residual review RR-P3-01 of the second review fix, checked against `main` with the Fanuc reader merged.
  - The finding no longer holds. With the Fanuc reader, an `O` section holding an `M30` that `M98 P100`, `M98 P0100`, `M98 P100 L2`, the older `M98 P20100`, `G65 P100` or the Nakamura `M200 P20100` enters is a `SUB` whose `M30` is `JUMP=END` (virtual machine 3.6), and `check` finds neither `VM071` nor another ERROR (language 4.13). Tests: `FanucSectionKindTests.SectionKind_OSectionHoldingM30EnteredByACall_IsASubWhoseM30IsJumpEnd` (one case per call form), `SectionKind_OSectionHoldingM30EnteredByNakamuraM200_IsASub`.
  - The follow-up held except for a `G65 P` of more than four digits. `FanucMacro.CalledProgram` read the older form of `M98` (the count in front of the program number) for `G65` and `M198` as well, so `G65 P90001` put program 1 into `SourceStructure.Calls` and wrote `CALL=1`: `O0001` became a `SUB` that calls itself, and `O90001`, uncalled and holding an `M30`, became the program. Controllers fanuc.md 1 and 7 and controller-mapping 6 give the older form to `M98` alone (the Nakamura `M200 P{times}{nnnn}` is "same as `M98`"), `G65 P9010` calls program 9010, and `Oxxxx` has eight digits on the 30i. Fixed: the older form is read for an `M98` or `M200` without `L`, the codes `Repeats` already took the count from, so `G65 P90001` enters `O90001` and `M198 P90001` calls `"O90001"`. The `TODO(question)` of `CalledProgram` now cites wave-2 question #69 and names `M98` and `M200`. Tests: `SectionKind_G65WithFiveDigits_EntersTheOSectionOfThoseDigits`, `SectionKind_M198WithFiveDigits_CallsTheExternalProgramOfThoseDigits`.
  - `DecideKinds` cites language 4.13, virtual machine 3.6 and 3.9 and controller-mapping 1 and 6 for the rule, and its `TODO(question)` cites wave-2 question #18, which widens #14; the workaround is unchanged. The documentation of `SourceStructure.Calls` names the older form and `G65`.
  - Files: `src/Ncx.Readers/StructurePass.cs`, `src/Ncx.Readers/SourceStructure.cs`, `src/Ncx.Readers/Fanuc/FanucMacro.cs` and `tests/Ncx.Readers.Tests/Fanuc/README.md` (both in the folders of the Fanuc reader, changed only for the follow-up), `tests/Ncx.Readers.Tests/Fanuc/FanucSectionKindTests.cs` (new).
