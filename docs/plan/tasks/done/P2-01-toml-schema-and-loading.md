# P2-01 TOML schema and loading

Phase: 2 | Milestone: M3 | Depends on: `P0-04` | Size: M (S: a day or two, M: up to a week, L: more)

## Goal

`Ncx.Config`: the machine file loaded with Tomlyn into the typed records of `Ncx.Core` (D107), every mistake reported with its line.

## Scope

- Records for every table of `machine-config.md`, in `Ncx.Core` (namespace `Ncx.Core.Machine`) and loaded by `Ncx.Config` (D107): `[machine]`, `[format]`, `[tool_change]`, `[home]`, `[setpos]`, `[roles]`, `[[resource]]`, `[[axis]]` with `programming`, `max_feed`, `acceleration`, `home` and `limits` in machine coordinates, `letter`, the `[positions]` table of named machine-frame positions (`tool_change`, `program_end`, ...) that expansion rules reach through `{position:NAME}` (D100), `[dynamics]`, `[diameter]`, `[spindle.ROLE]`, `[spindle_mode.ROLE]`, `[spindle_sync]`, `[workpiece]`, `[sync]`, `[coolant]`, `[func]`, `[func_meta]`, `[transform]` with the tilt, retract and rotary keys, `[retract]`, `[tolerance]`, the expansion rule keys, `[cycles]` and `[[cycle]]`, `[variables]`, `[system_variables]`, the optional `tool_table` and `[[node]]` tree (loaded, not interpreted).
- Validation: unknown keys are WARNINGs (the schema is a sketch until the compiler exists), missing required keys and wrong types are ERRORs, more than one resource of a kind without a default is an ERROR (VM 3.8).
- `DefaultMachine` (D103): a `MachineConfig` built in code with one work spindle `MAIN` with the C axis, one tool holder `TOOL` with the tool spindle `TOOL`, axes `X Y Z A B C` without limits and without reference points, coolant channel `STANDARD`, no named functions, the arc tolerance of D36 and units `MM`; `check`, `trace`, `annotate` and `analyze` use it when no machine file is given.
- Function values are strings; a bare integer is accepted and means `M` followed by the number (the one exception to the wrong-type ERROR above). The loader normalizes every M or G code to its canonical spelling without leading zeros (`8`, `08`, `M8`, `M08` become `"M8"`, `G01` becomes `"G1"`), the compiler always writes the normalized form, readers compare codes by number (D105).
- The job manifest (`<name>.ncxjob.toml`) with `[job]` naming the machine, `[shared]` listing the shared spindles and axes, and `file` and optional `program` per channel (D48), and the vars file.

## References

- machine-config.md sections 1 to 9
- architecture.md section 6 (MachineConfig class diagram)

## Done when

- The five example machine files (with `millturn1.toml`, D104) load without ERROR; the WARNINGs, if any, name real gaps in the files (fix the files).
- A file with a typo in a key reports the line and the nearest known key.

## Log

(who, when, what was decided while doing it; a decision that changes the specification gets a row in `../../../decisions/decisions.md`)

Claude (agent), 2026-09-13. Built on `main` at P2-04a (P0-01, P0-02 and the D100 machine data merged). All of section P2-01 of `implementation/12-phase-2-configuration.md` except `TemplateSet` and `FindFunctionByCode`, which P2-02 builds. Decisions implemented: D105 (function values), D103 (`DefaultMachine`), D107 (the records in `Ncx.Core.Machine`), D100 (`home`, `limits`, `[positions]`, `[dynamics]` loaded), D56 (`channel`, `channels = "all"`), D98 (`CFG` codes), D15, D20, D48 (job manifest). The specification is unchanged apart from the document fixes F23 and F24; everything below is a reading of it.

- Records in `src/Ncx.Core/Machine/`, one file each, sealed, `required` where the loader always sets them, template values as text: `MachineConfig`, `MachineIdentity`, `OutputFormat`, `BlockNumbering`, `ToolChangeConfig`, `HomeConfig`, `SetposConfig`, `ResourceDef` (with `Channel`, F23), `AxisDef`, `PositionsTable`, `DynamicsConfig`, `DiameterConfig`, `FunctionTable` (states, `Channel`, `AllChannels`, `Rule`, and `RpmMin`, `RpmMax`, `AccelTime` of a spindle table), `ExpansionRule`, `FuncMeta` (keyed `FUNCTION.STATE`), `WorkpieceConfig`, `SyncConfig`, `MarkRange`, `SyncGroup`, `TransformTable`, `RetractTable`, `ToleranceTable`, `RawTable` (F23), `CyclesConfig` (the catalog file name only), `VariablesConfig` (defaults of VM 3.6), `SystemVariables`, `KinematicTree`, `KinematicNode`, `JobManifest` (`Machine`, `SharedAxes`, F24), `ChannelProgram`; enumerations `Controller`, `GcodeSystem`, `LimitPolicy`, `ResourceType`, `AxisKind`, `Programming`, `PathMode`, `PreloadPosition`, `ProgramLayout`, `WorkpieceFrame`, `SyncPaths`, `UnassignedVariable`. `MachineIdentity.UnitsDefault` stays the word `MM` or `INCH` (TODO in the file): a second `Units` type next to the one of the channel state (P1-01) would collide wherever both namespaces are used. `MachineConfig` has `FindResource`, `FindAxis`, `ResolveRole`, `ResolveDefaultSpindle` and `ResolveDefaultHolder` (the default, or the only resource of the kind, VM 3.8 rule 2), `ResolveAxis(name)` and `ResolveAxis(name, workpieceHolder)` (rule 3), `FindFunction(name, state)`.
- `Ncx.Config`: `MachineConfigLoader.Load(path, diagnostics)` and `LoadText(text, diagnostics)`, null when the file has an ERROR, one part per group of sections. Tomlyn 2.10.1 reads the text twice (`TomlDocument`): `SyntaxParser.Parse` with validation reports every syntax error with its line (the model alone silently keeps the last of a duplicated key) and gives the header line of every `[[table]]`; `TomlSerializer.Deserialize<TomlTable>` with a `TomlMetadataStore` gives the line of every key, dotted keys and keys of inline tables included. `ConfigTable` walks one table: typed reads, a wrong type an ERROR on its line, a missing required key an ERROR on the line of the table it names, an unknown key a WARNING on its line with the nearest known key (`NearestKey`: edit distance with capitals not counted; ties go to the nearer capitals, then the first). `JobManifestLoader`, `VarsFile` (numbers and strings as `IntegerValue`, `DecimalValue`, `StringValue` by name; the `VariableStore` of P1-01 takes them as its seed), `DefaultMachine.Create()`. Codes `CFG001`-`CFG007` in `DiagnosticCodes.Loading.cs`, the main part with the ranges (`CFG001`-`CFG099` loading, `CFG100`-`CFG149` templates).
- Function values (D105): the states of `[spindle.ROLE]`, `[spindle_mode.ROLE]`, `[spindle_sync]`, `[coolant]` and `[func]`; there a bare integer from 0 on and a string of digits mean `M` and the number. Every other template (`program_end`, `sub_end`, tool change, home, setpos, sync, transform, retract, tolerance, workpiece, diameter, axis clamp) is normalized too but takes strings only; NCX text in `pre` and `post`, `requires`, `restore`, `[raw] known`, `[variables] map` and `[system_variables]` stay as written. An M or G code token is the letter at the start or after a character that is not a letter, digit or underscore; the zeros before its number go (`G05.1` is `G5.1`), so `M140 MB`, `WAITM(`, `LIMS=` and `T01M06` are left alone.
- Readings of the schema: the state keys of the spindle, spindle mode, synchronization and coolant tables are the values of their word, other keys there are unknown; the states of a `[func]` entry are free. The values of a placeholder (`kind_map`, `[transform] move`, `[tolerance] mode`) are strings or whole numbers kept as text, as machine-config 3 writes the kinds as numbers. A word outside the values its key takes (`controller = "fanox"`) is an ERROR of its own code, `CFG005`. The nearest known key is always named, as the task asks, even for a real gap (`CLAMP` is nearest to `CW`). `[[cycle]]` in a machine file is known and not loaded yet (TODO for P2-03). `[machine] kinematics` is kept in `KinematicTree.File`.
- Open questions, marked `TODO(question)`: which keys are required (machine-config names none; the loader requires `[machine]` `name` and `controller`, `id` and `type` of `[[resource]]`, `id`, `ncx` and `kind` of `[[axis]]`, `id` of `[[node]]`, `[job]` `machine`, `id` and `file` of `[[channel]]`); "of a kind" in VM 3.8 rule 2 (spindles counted together for `default_spindle`, tool holders for `default_holder`); whether A and B also resolve to the C axis of the workpiece holder (rule 3; only the holder axis's own letter does); the role `TOOL` of D103, which names both the holder and the tool spindle (the tool spindle gets it); the default spindle of the default machine (the tool spindle); the missing key for the arc tolerance of D36; the address of the Doosan sub spindle C axis.
- Document fixes: F23 in machine-config 3 (`kind_map`, already in architecture 6), 4 (`channel` on `[[resource]]`), 5 (`[raw]`, and one sentence on the states of each function table); F24 in machine-config 8 (`[shared] axes`) and architecture 6 (`JobManifest.Machine`, `SharedAxes`, and `MachineConfig.Raw` for F23). The CLI table of architecture 10 is P6-01's part of F24.
- Shared files: `tests/Ncx.Config.Tests/Ncx.Config.Tests.csproj` embeds `Fixtures/expected-machine-warnings.txt` (implementation 00-method 4); `docs/spec/examples/machines/doosan-puma-2600sy.toml` (P2-04's file) declares the axis `C2` that its resource `S2` names, the F23 gap of the Nakamura file that P2-04a found and left, because it is an ERROR under F23; `src/Ncx.Config/Placeholder.cs` deleted, as its TODO asked.

Done when:

- The five example machine files (with `millturn1.toml`, D104) load without ERROR; the WARNINGs, if any, name real gaps in the files: holds (`ExampleMachine_EveryFile_LoadsWithoutError`; the three mills of `machines/` too). The eight WARNINGs are listed in `tests/Ncx.Config.Tests/Fixtures/expected-machine-warnings.txt` (`MachineFiles_Warnings_AreTheCommittedList`): `CLAMP` inside the spindle tables of `mori-ntx1000-mapps.toml` and `dmg-ctx-840d.toml`, and `C_SYNC` inside the `[spindle_sync]` of the Mori Seiki, which the Nakamura and Doosan files write as named functions in `[func]`. Fixing them in the files is P2-04's (phase 2, P2-01 tests), which empties the list.
- A file with a typo in a key reports the line and the nearest known key: holds (`UnknownKey_TableFormt_WarnsOnItsLineAndSuggestsFormat`, `UnknownKey_TypoInASpindleTable_WarnsWithTheNearestKey`).

Gate: `dotnet build -warnaserror` with 0 warnings, 192 tests passing (85 in `Ncx.Core.Tests`, 97 in `Ncx.Config.Tests`), `dotnet format --verify-no-changes` clean.
