# P3-06 Fanuc compiler

Phase: 3 | Milestone: M6 | Depends on: `P3-03`, `P3-05` | Size: L (S: a day or two, M: up to a week, L: more)

## Goal

NCX to Fanuc G-code, per section 10 of `controllers/fanuc.md`.

## Scope

- `%`, `O` with the name, `N` numbers per `[format]`, modal G only on change, `G90` header or `G91` blocks, `I J K` or `R`, sweeps split into turns, `G43 H`/`D`, `T`/`M6` per `[tool_change]`, `M3 S` in one block, `G54`, `G52`, `G53`, `G28` for `HOME`, `G50`/`G92` for `SETPOS`, `G68`, `G51.1`, `G68.2`/`G53.1`, `G43.4`, cycles `G81`.. with `R` and `Z` from the absolute words and `G98`/`G99` from `CYCLE_RETRACT`, `G80`, `O` subprograms after `M30`, `M98`, `GOTO`/`IF`/`WHILE` from the flow words, `#` variables, the wait marks from `[sync]`, `program_end`.
- The G-code system of the target (`U W`, `G98`/`G99` in system A).

## References

- controllers/fanuc.md section 10
- controller-mapping.md (Fanuc column)
- examples/sources/2.5D_FRAESEN.fanuc.nc

## Done when

- `2.5D_FRAESEN.ncx` compiles to a program equivalent to `2.5D_FRAESEN.fanuc.nc`; the round trip Fanuc to NCX to Fanuc reproduces the source under the comparison rules; the same for `BOHREN` and `3D_FRAESEN` (M6).
- This closes phase 3.

## Log

(who, when, what was decided while doing it; a decision that changes the specification gets a row in `../../../decisions/decisions.md`)

- Claude (agent), 2026-09-14: section P3-06 of `13-phase-3-readers-compilers.md` in `src/Ncx.Compilers/Fanuc/` (namespace `Ncx.Compilers.Fanuc`) on the framework of P3-03, registered in `ncx compile` (`src/Ncx.Cli/Program.cs`); tests in `tests/Ncx.Compilers.Tests/Fanuc/` and `tests/Ncx.Acceptance/Examples/FanucCompilerTests.cs`.
  - Files: `FanucCompiler` (the dispatch of a block: the frame, RAW and native blocks, then label, chain, tools, spindle lines, transformations, modal words, cycles, motion, verbs, offsets, functions, dwell, flow, sync, comment), `FanucProgramFrame`, `FanucMotion`, `FanucArcs`, `FanucToolWords`, `FanucFunctions`, `FanucFrames`, `FanucCycles`, `FanucFlow`, `FanucSync`, and the helpers `FanucLine` (the order of the words of a block), `FanucBlock`, `FanucLabels`, `FanucAxes`, `FanucCodes` (the modal groups of fanuc 3 per G-code system), `FanucExpressions` (`#` variables and macro expressions). The G-code system of the target: system A writes `U W H V`, `G98`/`G99` as the feed mode, `G50` for `SETPOS` and no `G90`/`G91`; system B writes `G91` blocks and `G77`..`G79` for the turning cycles, which are an ERROR in system C (D174).
  - Codes CMP300 to CMP420 in `DiagnosticCodes.Fanuc.cs`, inside the range CMP300-CMP499 of the Fanuc compiler.
  - Decided, for the habits of the Fanuc sources that no document fixes: four `[format]` options rather than special cases (implementation 13, Risks). Each is a new key and so marked `TODO(question)` in `OutputFormat`, `MachineConfigLoader.Identity.cs` and `machines/fanuc-mill-30i.toml`: `header` (the lines of the start block, `"G0 G40\nG80 G90 G94 G98"` on the mill; without it `G90`, and nothing in system A), `plane_with_first_motion` (`G17` in the first motion block, not in the header), `motion_code_after_tool_change` (`G0` again after `M6`), `length_offset_with_tool_axis` (`G43 H` with the first move of the tool axis).
  - Other open questions, marked `TODO(question)` with the least committal workaround: the order of the words of a block (`FanucLine`); `G2`/`G3` on every arc block; `G91` blocks for incremental words outside system A; `G91` in front of the `[home]` template; a program without `NUMBER` (CMP300); the block numbers of identifier labels and of the end a conditional `JUMP=END` names; `REPEAT` (CMP304); more than three M codes in a block; CSS of a spindle whose table has no `VC`/`CSS_OFF` (CMP010, the sub spindle of `nakamura-ntjx.toml`); the block numbers and the place of the contour of `G70`..`G76` (CMP384, so the multiple repetitive cycles are not written).
  - Followed the recommendations on record, each cited at its `TODO(question)`: D34, D119, D144, D154, D159, D160, D173, D174, D177, D189, D210, D215, D217 (in the acceptance test), D218, D222, D242, D243, D245, D247, D248; D212, D213 and D246 are cited where they apply.
  - Shared files: `OutputFormat.cs` and `MachineConfigLoader.Identity.cs` (the four keys), `machines/fanuc-mill-30i.toml` (their values), `src/Ncx.Cli/Program.cs` (the registration), `tests/Ncx.Acceptance/Cli/CompileCommandTests.cs` (the CLI401 test uses a registry with the Heidenhain compiler alone, since Fanuc is now registered).
  - Measured (Risks: `3D_FRAESEN` is the performance gate of the chain): the Fanuc reader, NCX writer, parser, VM with snapshots and Fanuc compiler over `3D_FRAESEN.fanuc.nc` take 117 to 134 ms in process after a warm-up run (Debug build). `Chain_3DFraesen_TakesWellUnderASecond` holds the chain under a second.
  - Review fixes (Claude (agent), 2026-09-14, commit "P3-06 review fixes"), each with a test:
    - `length_offset_with_tool_axis`: a `CYCLE_CALL` drills along the tool axis, so a waiting `G43 H` stands in the cycle block (`G43 G81 G99 Z-20. R5. H1 F100.`). `HOME` and `G53` moves end at machine positions, which the offset does not move, so they leave it waiting. It is written in a line of its own before a `LABEL`, `JUMP`, `CALL` or `RAW` block, before a skipped block that moves the tool axis, and before the end of a program or subprogram, so that no path runs without it and none is dropped. This is part of the `TODO(question)` of `OutputFormat`.
    - `TCPM=OFF`: `G49` also cancels the length offset (fanuc 4), and the holder keeps `OFFSET:LEN` (language 4.4, VM 4). So `G43 H` stands again: with the next move of the tool axis under the option, right after `G49` without it.
    - A combined `OFFSET` in a `TOOL` block is marked written only when the change template in use has `{offset}`. On the mill (`T{tool} M6`) it is CMP308, not dropped.
    - Block numbers: a number that a line carries itself (the label `N20`, the end `N6 M30`) is given to no other line of the file, and `block_numbers` goes on past it. This is a new `TODO(question)` in `FanucCompiler.OwnBlockNumber`. The shared hook is `CompilerBase.OwnBlockNumber`, passed to `OutputBuffer`.
    - Real numbers (fanuc 2, 10 rule 5): `FanucBlock.Format` writes the decimal point on every address except the whole-number addresses `P L K S H D T M G N O`, whether `[format] decimals` lists the address or not (`R30.`, `K1.`, `F` too). The real values the compiler gives a template carry the point unless the template writes one after the placeholder (`{b}.`, machine-config 3). These are the origin and angles of `TILT_ON`, the radius of `CYLINDER`, `RETRACT`, `TOLERANCE`, and the angles of `ORIENT` and `PHASE`. The zeros of the fixed forms of fanuc 4 and the sources (`G68 X0 Y0`, `G52 X0 Y0`, `G28 X0 Y0`) stay as they are.
    - Changed test expectations, with no expected-output file touched: `FanucFramesTests` (`R30.`, `G68.2 X0. Y0. Z0. I0. J45. K0.`, `G7.1 C30.`, `I0. J0.5 K0.866`, and `G43 H1` after `G49`).
  - Done when: both criteria hold. `2.5D_FRAESEN.ncx` compiles to a program equal to `2.5D_FRAESEN.fanuc.nc` under the comparison rules except N380 and N390. There, the example's moves of note 4 (`RAPID ... FRAME=MACHINE`) are written as `G53 Z0.` and `G53 X0. Y0.`, against the source's `G91 G28 Z0` and `G28 X0 Y0` (D217; `Compile_25DFraesenExample_EqualsTheSourceExceptTheMovesOfNote4`). The round trips Fanuc to NCX to Fanuc reproduce `2.5D_FRAESEN`, `BOHREN` and `3D_FRAESEN` under the comparison rules (`RoundTrip_FanucSource_ReproducesTheSource`); the comparison is the test's own until the `NcComparer` of P3-07 exists. Closing phase 3 rests with P3-07.
  - Second review fixes (Claude (agent), 2026-09-18, commit "P3-06 review fixes"), each with a test; the branch was rebased onto `main` first:
    - A sweep beyond 360 degrees (`FanucArcs.WriteTurns`) writes `G90` only outside G-code system A. In system A `G90` is the simple turning cycle, not a distance code, and the turn points already stand as the absolute letters `X Z` (fanuc 3). Test: `FanucSystemATests.ArcAngle_OnALatheOfSystemA_WritesTheTurnsWithoutG90`.
    - The factor of an expression value (`FanucExpressions.WordValue`) is written as the constant it is: `* 2` or `/ 2` between radius and diameter (D60), `* 1000` from the seconds of `DWELL` and `CYCLE_DWELL` to the milliseconds of `P` (language 4.1, 4.7; fanuc 4, 6; D159). Before, every factor other than 1 was taken as 2. An operation is now bracketed before the factor, `[[#1 + 0.5] * 1000]`, because the old form `[#1 + 0.5 * 2]` multiplied only the last operand. Tests: `FanucMotionTests.Dwell_AsAnExpression_IsG4PWithTheExpressionTimes1000`, `FanucCyclesTests.CycleDwell_AsAnExpression_IsPWithTheExpressionTimes1000`.
    - `length_offset_with_tool_axis`: `SETPOS` declares the position of the tool axis with the length offset active (language 4.2, SETPOS; 4.4; 5 rule 3). So an offset that waits for the tool axis stands in a line of its own before the `G92` line of a `SETPOS` that has a word of the tool axis. This covers an offset from an earlier block and the `OFFSET:LEN` of the `SETPOS` block itself (`G43 H1`, `G92 Z100.`). A `SETPOS` of the other axes leaves the offset waiting. `FanucMotion.DeclaresToolAxis` names the case, and the `OutputFormat` description and its `TODO(question)` name it too. Tests: `FanucToolWordsTests.LengthOffset_WaitingBeforeASetposOfTheToolAxis_IsWrittenBeforeIt`, `LengthOffset_InTheBlockOfASetposOfTheToolAxis_IsWrittenBeforeIt`.
    - No expected-output file and no existing test expectation changed. Shared file: `src/Ncx.Core/Machine/OutputFormat.cs` (the description of `length_offset_with_tool_axis` and its `TODO(question)`).
    - Done when: both criteria still hold, as recorded above.
  - Third review fixes (Claude (agent), 2026-09-18, commit "P3-06 review fixes"), each with a test. The branch was rebased onto `main` (P3-04 Heidenhain compiler) afterwards. The one conflict, the registration in `src/Ncx.Cli/Program.cs`, is resolved in the first commit, which now registers both compilers, one line each:
    - A `LABEL` that a `JUMP` names (`FanucLabels.IsJumpTarget`) makes the target state unknown after its `N` line (`FanucBlock.MakeTargetUnknown`). The STATIC walk does not follow `JUMP` (VM 1), so the target state at the label is that of the block above it, and a jump arrives with the codes of its own block. Every modal code (`G0`/`G1`, `G90`/`G91`, the plane, `G94`, `F`, `D`, `S`) stands again at its next use, and `G80` before the next motion. The label of the `M99` loop keeps its state, since the start block runs again. Test: `FanucFlowTests.Label_ThatAJumpReaches_WritesTheModalCodesOfTheNextMotionAgain`.
    - A skipped block (`/`, `/n`) reaches the control only while the switch is off (controller-mapping 1, SKIP). Every key of the target state that the block changed is made unknown before the next block of its program or walk (`FanucCompiler.MakeSkippedUnknown`). For a skipped `CALL`, this includes what the subprogram's walk wrote. A waiting length offset now stands unskipped before a skipped block that sets `OFFSET:LEN` or `TCPM` as well, since such a block would take it. Tests: `FanucMotionTests.Skip_LineAfterASkippedLine_WritesItsCodeAndFeedAgain`, `Skip_IncrementalLineAfterASkippedOne_WritesG91Again`, `Skip_CallOfASubprogram_LeavesWhatTheSubprogramWroteUnknown`.
    - `G90` stands in front of `G52` (set and cancel), `G68 X0 Y0 R`, `G51.1` and the `TILT_ON` line, and in the `[setpos]` block (`G90 G92 X0. Y0.`), wherever the control may have `G91` from `HOME` or an incremental block, outside system A (language 2 rule 3). The reader, and the control as D244 (b) and D245 read the manual, take their words as increments under `G91`, and `G90` is right whatever those entries answer. A `SETPOS` whose every word is incremental is written under `G91`, as the reader reads `G91 G92`. One that mixes both forms is CMP323 (new `TODO(question)` citing D117). Tests: `FanucFramesTests.Setpos_AfterHome_IsG92UnderG90`, `Chain_AfterAnIncrementalMove_IsWrittenUnderG90` (G52, G68, G51.1), `Shift_ResetAfterAnIncrementalMove_IsG52X0UnderG90`.
    - `G80` stands before a motion, `HOME` or tool change where the target state does not know that no cycle is active: in a subprogram, which is written from an unknown target state (D99), and after a label that a jump reaches. `G80` without an active cycle changes nothing. After the return the caller then has `G80`, and its next `CYCLE_CALL` writes the definition again (the `TODO(question)` of D247 stays). Test: `FanucCyclesTests.Call_OfASubprogramBetweenTwoCycleCalls_EndsTheCycleAndDefinesItAgain`.
    - Representation, decided (not observable beyond the output above): an unknown that must reach the caller of a subprogram is kept in `TargetState` as the value `"?"`, which no code equals (`FanucBlock.MakeUnknown`). A removed key would be taken over as the caller left it (`TargetState.TakeOver`). The motion code that `G80`, a cycle definition and `motion_code_after_tool_change` leave to be written again uses it too. So a caller whose subprogram ends that way writes its `G0`/`G1` again. `TargetState` itself is unchanged.
    - `PITCH` of a tapping cycle without `CYCLE_F`, in feed per minute, where the speed is not known (an `RPM` from an expression, which the VM keeps UNKNOWN, or no speed) is the new ERROR CMP386 (`FanucTapFeedNotKnown`), never a `G84` without `F` (controller-mapping 5, TAP; language 2 rule 8). A `PITCH` expression with a known speed is written as `F[#2 * 1000]`. Tests: `FanucCyclesTests.Tap_WithPitchAndASpeedFromAnExpression_IsTheErrorCmp386`, `Tap_WithAPitchExpression_FeedsTheExpressionTimesTheSpeed`.
    - `TILT` or `TILT_AXIS` with `MOVE=MOVE` is the ERROR CMP308, no longer written as `MOVE=STAY` with a WARNING. `G68.2` alone is `STAY` and `G53.1` is `TURN` (controller-mapping 1, MOVE), and language 4.2 (D82) makes a form the target cannot write an ERROR. Test: `FanucFramesTests.Tilt_WithMoveMove_IsTheErrorCmp308`.
    - Changed test expectations, no expected-output file touched: `FanucFlowTests.Label_UnderBlockNumbers_KeepsItsNumberForItself` and `FanucToolWordsTests.LengthOffset_WaitingAtALabel_IsWrittenBeforeTheLabel` (`G80` and the modal codes after the label), `FanucWriterRulesTests.Rule3_ProgramEndAndSubprograms_StandAfterTheEndAsOPrograms` (`G80` before the first motion of `O0100`). No shared file changed.
    - Measured again: the chain over `3D_FRAESEN.fanuc.nc` (reader, writer, parser, VM, compiler) takes 139 to 163 ms in process after a warm-up run (Debug build).
    - Done when: both criteria still hold. The round trips of `2.5D_FRAESEN`, `BOHREN` and `3D_FRAESEN` and the compile of `2.5D_FRAESEN.ncx` are unchanged, since the sources have no label, skip, subprogram or frame under `G91`.
  - Residual defects of the final review (Claude (agent), 2026-09-18, commit "RF-P3-06 Residual defects of the Fanuc compiler"), each with tests that fail on `main` and pass after (26 of the 28 new or renamed tests; `Skip_FirstMotionSkipped_WritesThePlaneAgain` and `Call_AfterALabelWithTheSameCycleOnEveryPath_DefinesItAgain` guard the values that must still stand again and pass on both):
    - F outside RAPID, LINE and ARC was CMP308 (language 4.3: F is modal; 5 rule 2: F needs no verb). A block without a verb writes F in its line, `F200.`, `G95 F0.1` (fanuc 2, 4), as the Fanuc reader reads such lines; a `CYCLE` block the same, `F300.` before the cycle block. In a `CYCLE_CALL` (and `HOME`, `SETPOS`, `SHIFT`, `TILT`, `TILT_AXIS`, `RETRACT`) the F of the cycle block is the cycle's feed on the control (D29, D218), so the next LINE or ARC writes it, as the Heidenhain compiler does (`FanucMotion.WriteFeedWord`). Tests: `FanucMotionTests.Feed_InABlockOfItsOwn_IsFAlone`, `FeedMode_WithAFeed_IsG95WithF`, `FanucCyclesTests.Call_WithAFeed_LeavesTheFeedToTheNextLine`, `Cycle_WithAFeed_WritesTheFeedAlone`.
    - After a `LABEL` that a jump reaches and after a skipped block, the next block wrote the values of the STATIC walk for modal state it does not state (F, the plane, `G94`/`G95`, `D`, `S`), which are the values of one path only (virtual machine 1; D53). Now `FanucPaths` compares those values of NCX over the paths that meet there: the label's block before it and every `JUMP` to it (from the steps of the run), or the paths with and without the skipped block. A value that is the same on every path stands again at its next use, as before, because the control may hold another (the feed of a cycle, D218; the plane under `plane_with_first_motion`). A value that differs is left to the control (`FanucBlock.Hold`, the target value `=`): a block writes it only where it states it. For that the control must hold the value of each path, so F, the feed mode, the plane and the S of the spindle selected last are written in a line of their own before the label, before the `GOTO` of each jump and before a skipped block, where the control has another value (`F100.` after a cycle). Where that cannot be done (the `D` register, an S after another spindle's M code, a value from an expression the control lost), the value is `!` (`FanucBlock.Lose`): the next block that takes it without stating it is the new ERROR CMP309 (`FanucModalValueNotHeld`), and so is a jump that arrives with such a value. The active cycle is compared the same way: a `CYCLE_CALL` whose cycle of NCX differs by path is CMP309, since Fanuc defines a cycle with its first call. A skipped `WORKPLANE` now writes its plane in its own line under `plane_with_first_motion`. The same rule removes a stale F: a LINE after `F={$V1}` wrote the F the walk kept from before (`F0.`); it now writes none, the control holds the value, and CMP309 where a cycle's F replaced it. The review's own proposal (write only what the block states) would have dropped values the control does not hold, so the fix keeps the walk's value where it is the same on every path. Changed test expectation: `FanucMotionTests.Skip_LineAfterASkippedLine_WritesItsCodeAndLeavesTheFeedToTheControl` (was `..._WritesItsCodeAndFeedAgain`, which asserted `G1 X5. F50.`; now `G1 X5.`). New tests: `FanucMotionTests.Skip_FirstMotionSkipped_WritesThePlaneAgain`, `Skip_FeedModeOfASkippedBlock_LeavesTheFeedModeToTheControl`, `Skip_AfterTheFeedOfACycle_WritesTheFeedOfNcxBeforeTheSkippedBlock`, `Skip_WhereTheControlHoldsNoFeedOfNcx_IsTheErrorCmp309`, `Line_AfterAFeedFromAnExpression_WritesNoFeedOfTheWalk`; `FanucFlowTests.Label_ThatAJumpReachesWithAnotherFeed_LeavesTheFeedToTheControl`, `Label_ThatAJumpReachesInAnotherPlane_LeavesThePlaneToTheControl`, `Label_WhereTheControlHasTheFeedOfACycle_WritesTheFeedOfNcxBeforeIt`, `Jump_WhereTheControlHasTheFeedOfACycle_WritesTheFeedOfNcxBeforeTheGoto`, `Jump_WithASpeedTheControlCannotTake_IsTheErrorCmp309`; `FanucToolWordsTests.Skip_SpeedOfASkippedBlock_LeavesTheSpeedToTheControl`, `Skip_RadiusOffsetOfASkippedBlock_LeavesTheRegisterToTheControl`; `FanucCyclesTests.Call_AfterALabelThatAJumpReachesWithAnotherCycle_IsTheErrorCmp309`, `Call_AfterASkippedCycleBlock_IsTheErrorCmp309`, `Call_AfterALabelWithTheSameCycleOnEveryPath_DefinesItAgain`.
    - Every cycle word was marked written at the `CYCLE` block, so a word the catalog entry does not map was dropped: `PITCH` of `THREAD` (`G92` has no parameter in `cycles/fanuc.toml`), `CYCLE_F` of an entry without `F`, `SAFE` of an entry without `CLEARANCE`, and `DEPTH` and `CLEARANCE` were written as Z and R whether mapped or not (R is the taper of `G90`). Now each word of a `CYCLE` block must be one the entry carries (`CycleEntry.Words`: its params, the rule words of the drilling family, `PITCH` of `TAP` with an F address); any other is CMP385 at the `CYCLE` block (language 2 rule 8; machine-config 6), and Z and R stand only where the entry maps them. A new `TODO(question)` in `FanucCycles.CheckCarried` cites D157 and D175. Tests: `FanucCyclesTests.Thread_WithPitch_IsTheErrorCmp385`, `TurnOd_WithDepthAndClearance_IsTheErrorCmp385`, `Drill_WithPitch_IsTheErrorCmp385`.
    - The pitch feed of `TAP` in feed per minute took S from the default spindle, the main spindle of a lathe, whether it ran or not. S is now the speed of the spindle that turns the tap, the spindle of the current tool holder, else the default spindle (virtual machine 2.3, 5): the driven tool `TOOL` of the turret. A spindle that is off, in `AXIS` mode, under CSS or of unknown speed is CMP386 (controller-mapping 5, TAP). Tests: `FanucCyclesTests.Tap_OnTheDrivenToolOfALathe_FeedsWithTheSpeedOfTheDrivenTool`, `Tap_WhileTheSpindleOfTheHolderIsOff_IsTheErrorCmp386`.
    - The CSS lines ignored the S binding rule and the S they change. `G96 S140` carries the M code of its spindle where the control may have another spindle selected (`G96 S140 M3`, as `NAKAMURA_WY250L_O1000.path1.nc` writes `G96S120M03`; fanuc 5, 10 rule 2; controller-mapping 4), and the main line does not write that code again. `G96 S` gives the control's S the cutting speed, so the speed of RPM is unknown on the control after it. `CSS=OFF` of a running spindle writes `G97 S1000` (language 4.11: RPM applies again; fanuc 4), and a standing spindle writes its S with its next start. The limit `G50 S`/`G92 S` of `RPM_MAX` stays alone, with a new `TODO(question)` (whether the limit binds to a spindle is not in the documents). Tests: `FanucToolWordsTests.Css_AfterTheCodeOfAnotherSpindle_CarriesTheCodeOfItsSpindle`, `Css_Off_WritesTheSpeedOfRpmWithG97`, `Css_OffWhileTheSpindleStands_WritesTheSpeedWithTheNextStart`.
    - Files: new `FanucPaths.cs` and `FanucModalValue.cs`; `FanucBlock`, `FanucCompiler`, `FanucFlow`, `FanucMotion`, `FanucCycles`, `FanucToolWords`, `DiagnosticCodes.Fanuc.cs` (CMP309; the texts of CMP385 and CMP386), the folder README. No shared file and no expected-output file changed; the acceptance round trips of `2.5D_FRAESEN`, `BOHREN` and `3D_FRAESEN` and the tests of the Nakamura job pass unchanged.
  - Review fixes of the residual defects (Claude (agent), 2026-09-18, commit "RF-P3-06 review fixes"), each with tests that fail before and pass after (10 new tests, no existing expectation changed):
    - A `JUMP` that arrived with a `D` register or a speed that differs by path, which the control does not hold, was CMP309 at the jump, even where every block after the label states the value again. This rejected programs that `main` compiles right, the loop around a Heidenhain `TOOL CALL` above all (the reader writes `OFFSET:RAD` with every tool call). Now the jump side is lazy like the side of the block before the label. A jump written before its label tells the label what it brings (`FanucMeeting`), and the label leaves such a value to the control with `Lose`, so CMP309 falls on the first block after the label that takes it without stating it (`FanucBlock.NeedsValueOfTheWalk`). A jump back to a label written before it checks what the label decided: where the control does not hold the value of the jump, CMP309 stands at the jump only where a block from the label to the jump takes the value before a block states it (`FanucPaths.TakenAfterLabel`). Tests: `FanucFlowTests.Jump_WithASpeedStatedAgainAfterTheLabel_Compiles`, `Jump_WithARadiusRegisterStatedAgainAfterTheLabel_Compiles`, `Jump_ForwardWithARadiusRegisterNoBlockTakes_Compiles`, `Jump_ForwardWithARadiusRegisterABlockTakes_IsTheErrorCmp309AtThatBlock`; `Jump_WithASpeedTheControlCannotTake_IsTheErrorCmp309` is kept. The review's case of `BOHREN.h` (converted, `RAW` lines removed, wrapped in `LABEL=1` ... `JUMP=1 IF={$Q1 < 3}`) compiles for `fanuc-mill-30i.toml` again.
    - An S alone (`S1000`, `S2000`) was written before a label or a `GOTO` for a spindle under CSS, where `G96` makes it a cutting speed (language 4.11; controllers fanuc.md 4). Now the control holds no speed of a spindle that is under CSS or in `AXIS` mode on that path (`FanucPaths.CanHold`), and no S stands alone where the control has `G96` or may have it on another path. G96 or G97 is now a compared value of its own (`CSS:` with the spindle id, set by `CSS=ON`, `CSS=OFF` and the `G96 S` of `VC`). Where it differs by path, a start, an S and the `G96 S` of `VC` without `CSS` are CMP309 (`RPM=2000` after a label that the jump reaches under `G96` would be the cutting speed 2000). Tests: `FanucFlowTests.Label_ReachedUnderCss_WritesNoSpeedAlone`, `Jump_UnderCss_WritesNoSpeedAlone`, `Label_ReachedUnderCssOnOnePath_RpmAfterItIsTheErrorCmp309`.
    - The label's `MakeTargetUnknown` threw away a value that the control held per path (`=` from `AfterSkippedBlock`) or did not hold (`!`), where the walk values agreed. The next block then wrote the walk's value on the path that skipped the block (D53). Now a value the target holds as `=` or `!` before the label differs by path, and so does one a jump brings that way. For a jump back, the label looks ahead in the steps to the jump (`FanucPaths.DependsOnThePathAt`: a skipped block after which the value differs, a statement by an expression, or another label where it differs, back to the last block that states it). `AfterSkippedBlock` applies the same rule to a value held per path before the skipped block, and a value that depends on the path is never written alone before a label, a jump or a skipped block. Tests: `FanucFlowTests.Label_AfterASkippedBlockThatLeftTheFeedToTheControl_KeepsItThere`, `Jump_AfterASkippedBlockThatLeftTheFeedToTheControl_KeepsItThere`, `FanucMotionTests.Skip_AfterASkippedBlockThatLeftTheFeedToTheControl_KeepsItThere`.
    - Files: new `FanucPaths.Steps.cs` and `FanucMeeting.cs`; `FanucPaths.cs`, `FanucBlock.cs` (`IsPathValue`), `FanucLabels.cs` (the meetings and the look-ahead per label), `FanucFlow.cs` (comment), `FanucToolWords.cs` (the `CSS:` key), the folder README. No shared file and no expected-output file changed.
