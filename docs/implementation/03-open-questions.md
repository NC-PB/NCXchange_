# Open questions of wave 1

Status: 2026-09-13, asked the same day as the sixth round of `../decisions/rationale.md` (D108 to D182); no answer yet. A row gets the date of its answer when the entry is answered and the code has followed.

The tasks of wave 1 (P0-01 to P0-06, P2-01 to P2-04a, P1-01 to P1-03a and P4-01a) recorded 111 questions in their logs, each with a `TODO(question)` marker where the code or the data takes a workaround. Each question was checked against the whole documentation set. Where the documents already answer it, the Disposition column names the section and what the code must change, if anything. Where only the shape of the code is at stake, and no output, diagnostic, state or plugin surface depends on it, the column says "implementation". Every other question is drafted as a decision under "Still open" in `../decisions/rationale.md`; several questions can share one entry, and one question can need several. Until an entry is answered the code keeps the least committal workaround at the location in the last column; the answer, the specification change, the code and the removal of the marker go into one commit. The Question column is a one-line summary; the question in full is in the task log named in the Task column (the letter a or b is the part of the task). Sections are cited as the documents cite them (language 4.7 is `../spec/ncx-language.md` section 4.7, VM is `../spec/ncx-virtual-machine.md`).

| # | Task | Question | Disposition | Code location |
|---|---|---|---|---|
| 1 | P0-01 | The analyzers do not report CA1305 while InvariantGlobalization is true; which setting gives way? | D108 | `Directory.Build.props:17`; `.editorconfig:43` |
| 2 | P0-01 | Nothing enforces the 120-character line of code-guidelines 3.3 | D109 | `.editorconfig:145` |
| 3 | P0-01 | Who is the copyright holder in LICENSE? | D165 | `LICENSE` (placeholder "the NCXchange authors", no marker) |
| 4 | P2-04a | Are home, limits and [positions] of a diameter-programmed X axis diameters or radii? | D137 | `docs/spec/examples/machines/doosan-puma-2600sy.toml:55`, `mori-ntx1000-mapps.toml:63`, `nakamura-ntjx.toml:63`, `dmg-ctx-840d.toml:67` |
| 5 | P2-04a | Address of the sub spindle C axis on the Nakamura NTJX: C or C2=? | D179 | `docs/spec/examples/machines/nakamura-ntjx.toml:130` |
| 6 | P2-04a | Does a preload template use {tool} or {next}? | D150 | `machines/fanuc-mill-30i.toml:41`, `heidenhain-itnc530.toml:39`, `siemens-840dsl-mill.toml:40` |
| 7 | P2-04a | Which tolerance template applies when TOLERANCE:ROTARY is not set? | D151 | `machines/heidenhain-itnc530.toml:130`, `siemens-840dsl-mill.toml:136` |
| 8 | P2-04a | Which role does the tool holder of a mill get? | D128 | `machines/fanuc-mill-30i.toml:53`, `siemens-840dsl-mill.toml:52`, `heidenhain-itnc530.toml:47` |
| 9 | P2-04a | Does the iTNC 530 accept TOOL CALL S without a tool number? What is a generic ORIENT on Heidenhain? | D180 (TOOL CALL S); the ORIENT half is answered by controller-mapping 4 (ORIENT row: "M function with angle, TOML") and heidenhain.md 4 (M19 is the builder's): leaving ORIENT out is right, the marker at line 113 becomes a plain comment citing them; the ORIENT half applied 2026-09-14 | `machines/heidenhain-itnc530.toml:112`, `:113` |
| 10 | P2-04a | Named TOOL on Siemens; the Siemens variable map; the Heidenhain FN 18 ids | D172 (named tools), D173 (variable map), D182 (FN 18 ids) | `machines/siemens-840dsl-mill.toml:39`, `:145`; `machines/heidenhain-itnc530.toml:139` |
| 11 | P2-04a | doosan-puma-2600sy.toml names axis C2 that no [[axis]] declares | answered by implementation 12 P2-01 ("a resource that names an undeclared axis is an ERROR", F23); already fixed on main by P2-01 (aee9b21 declares [[axis]] C2); code: none. The P2-04 part two log records it; the address letter is D179 | `docs/spec/examples/machines/doosan-puma-2600sy.toml` (fixed, no marker) |
| 12 | P2-04a | The Heidenhain sources have no M30 before END PGM, the compiler writes program_end = M30 | answered by D49 (program_end decides, the source's end form is not kept), D48 and heidenhain.md 8 rule 1, 7 rule 4; code: none. P3-04 adds one sentence to the "Comparison rules" of 13-phase-3: the program_end line is not a difference where the source ends in another form or in none | `machines/heidenhain-itnc530.toml` [format] program_end (no marker) |
| 13 | P2-03a | Is a key for the kind of a catalog parameter needed? | D115 | `cycles/fanuc.toml:5`, `cycles/heidenhain.toml:6`, `cycles/siemens.toml:6` |
| 14 | P2-03a | No key for the clearance behind the Fanuc SURFACE, nor for the unit of the G4 P | D158 (SURFACE clearance), D159 (G4 P unit) | `cycles/fanuc.toml:15`, lines 19-20 |
| 15 | P2-03a | Fanuc lathe drilling (G83..G85 along Z, G87..G89 along X): no key selects an entry by axis or machine kind | D160 | `cycles/fanuc.toml:79` |
| 16 | P2-03a | Turning cycle numbers of Fanuc G-code system C: G20/G21/G24 or G77/G78/G79? | D174 | `cycles/fanuc.toml:90` |
| 17 | P2-03a | How does the reader tell TURN_OD from TURN_ID, both G90? | answered by language 4.7.1 and controller-mapping 5 (G90, OD/ID, is one cycle TURN_OD) and implementation README (the specification wins over a phase file); code: remove the TURN_ID entry from `cycles/fanuc.toml` (lines 100-105) and the `InlineData("TURN_ID", true)` row from `ShippedCatalogsTests.cs`; correct P2-03 in 12-phase-2; applied 2026-09-14 | `cycles/fanuc.toml:105` |
| 18 | P2-03a | The Fanuc turning-cycle words beyond feed and contour (taper R, F of G92, the words of G70..G76) | D175 (the words), D161 (the NCX names of G92 and G76) | `cycles/fanuc.toml:92`, `:111`, `:122` |
| 19 | P2-03a | Do P and Q of G74..G76 name a contour? G73 and G74 have no NCX name | D176 | `cycles/fanuc.toml:123`, `:157` |
| 20 | P2-03a | Which Q211 makes cycle 200 a DRILL, which Q256 or Q213 makes cycle 203 a CHIP_BREAK? | D163 | `cycles/heidenhain.toml:42`, `:59` |
| 21 | P2-03a | Which value does the compiler write for a signature Q parameter no NCX word carries? | D164 | `cycles/heidenhain.toml:22` |
| 22 | P2-03a | Parameter lists of Heidenhain cycles 202, 204 to 206, 208, 209, 240, 241, 251 to 257; the feed of 206, the pitch of 209 | D178 | `cycles/heidenhain.toml:24`, `:87`, `:95`, `:108`, `:121`, `:138` |
| 23 | P2-03a | Should the machine-config 6 example of 251 list CLEARANCE in absolute_from_surface? | answered by heidenhain.md 5 (CLEARANCE = Q203 + Q200), language 4.7 (CLEARANCE row) and controller-mapping 5; code: none, the catalog is right. Document fix: the machine-config 6 excerpt reads `absolute_from_surface = ["DEPTH", "CLEARANCE"]` | `docs/spec/machine-config.md` 6 (no marker) |
| 24 | P2-03a | Siemens gaps: constant PECK on CYCLE83, MPIT, the feeds of CYCLE86, 830 and 840, _DP of CYCLE830, the signature tails | D181; the feeds are answered by siemens 7 ("The cycle feed is the modal F ... CYCLE85 has its own FFR"): add `CYCLE_F = "F"` to BORE (CYCLE86), DEEP_HOLE (CYCLE830) and TAP_COMPENSATING (CYCLE840) in `cycles/siemens.toml` and to BORE in `DrillingFamily.Siemens.cs`, and drop their three feed markers; CYCLE84 waits for D181; the feeds applied 2026-09-14 | `cycles/siemens.toml:25`, `:62`, `:72`, `:89`, `:100`, `:110` |
| 25 | P2-03a | Catalog file names: heidenhain-cycles.toml or cycles/heidenhain.toml? | D156 | `src/Ncx.Core/Machine/CyclesConfig.cs:9` (comment, no marker) |
| 26 | P2-03a | Are the catalog cycle names chosen in P2-03a acceptable; is 241 the same cycle as CYCLE830? | D162 | `cycles/heidenhain.toml`, `cycles/siemens.toml` (no marker) |
| 27 | P2-03a | Does a Fanuc control execute the dwell P on G81, G83, G73, G85 and G86? | D177 | `cycles/fanuc.toml:30` |
| 28 | P2-03a | No key lists the words an entry carries by a reader or compiler rule | D157 | `cycles/fanuc.toml:26` |
| 29 | P2-03a | Should G75 and G76 carry a contour range at all? | D176 | `cycles/fanuc.toml:157` |
| 30 | P0-02 | AddrKind has no kind for FUNC's function name, ARG's argument name and the ROTARY of TOLERANCE:ROTARY | implementation: the language rows decide, P0-03 added Function, Argument and ToleranceKind; only the None test decides behaviour (PAR150). The marker becomes a comment; document fixes: the enumeration line of architecture 4 lists the code's values, the ADDR row of language 3 adds a function name, an argument name, the fixed ROTARY and the builder dialect id; applied 2026-09-14 except the ADDR row of language 3, which stays for the maintainer | `src/Ncx.Core/Catalog/AddrKind.cs:3` |
| 31 | P0-02 | Scope lacks 'program' and the partner scopes of language 4 | implementation: P0-03 added None, Program and WithPartner, and only the generated table reads Scope; the enumeration line of architecture 4 follows the code. Whether a partner is enforced is D116; applied 2026-09-14 | `src/Ncx.Core/Catalog/Scope.cs` (marker removed in P0-03) |
| 32 | P0-03 | CENTER:IX before CENTER:X by address text, against bucket 5; CENTER:C and CENTER:IC have no rank | D143 | `src/Ncx.Core/Catalog/CanonicalRanks.cs:50` |
| 33 | P0-03 | @SAVE and @RESTORE have no bucket | D167 | `src/Ncx.Core/Catalog/CanonicalRanks.cs:161` |
| 34 | P0-03 | What kind of address is ROTARY in TOLERANCE:ROTARY? | implementation (as 30): AddrKind.ToleranceKind; accepting only ROTARY and refusing OFF on it follows from the 4.1 rows (PAR152, PAR153); applied 2026-09-14 | `src/Ncx.Core/Catalog/AddrKind.cs:3` |
| 35 | P0-03 | Is the role address of SPINDLE_MODE required? | D113 | `src/Ncx.Core/Catalog/SpindleWords.cs:38` |
| 36 | P0-03 | How does the parser accept the parameter names of a catalog cycle (LENGTH, WIDTH)? | D114 | `src/Ncx.Core/Catalog/WordCatalog.cs:111` |
| 37 | P0-05 | How does NOT appear in the canonical text? | D112 | `src/Ncx.Core/Expressions/UnaryNode.cs:22` |
| 38 | P0-05 | Does the canonical text keep parentheses the grammar does not need? | D112 | `src/Ncx.Core/Expressions/ParenthesesNode.cs:15` |
| 39 | P0-05 | Does Case (language 3) cover function names and AND, OR, NOT, MOD? | D112 | `src/Ncx.Core/Expressions/ExprTokenizer.cs:59` |
| 40 | P0-05 | How many arguments does each function take, and in which order does ATAN2 take them? | D118 (the count); the ATAN2 order is D119 | `src/Ncx.Core/Expressions/ExprParser.cs:307` |
| 41 | P2-02a | Is template text that cannot be read as placeholders an ERROR? | answered by code-guidelines 6 (a WARNING only where the specification allows it) and the machine-config introduction (an unusable template is reported); main matches (CFG101, CFG102); code: the marker becomes a comment citing them; applied 2026-09-14 | `src/Ncx.Config/Templates/DiagnosticCodes.Templates.cs:6` |
| 42 | P2-02a | What does Literal of Placeholder mean in architecture 6? | implementation: the class diagrams are the intended shape, not a contract (HANDOFF); Template keeps its literal text, Width and IsText cover either reading. The marker becomes a comment; the Placeholder box of architecture 6 becomes Name, Format, Width, IsText, and Template.Render and Matches take TemplateValues; applied 2026-09-14 | `src/Ncx.Config/Templates/Placeholder.cs:6` |
| 43 | P2-02a | Is the placeholder list of the machine-config introduction closed? | D152 | `src/Ncx.Config/Templates/Placeholder.cs:10` |
| 44 | P2-02a | Which placeholders hold words rather than numbers? | answered by the machine-config introduction ({name}, {axis}, {axes}, {position:NAME}), 3 ({axes} is "U0 W0") and 5 ({move}, {channels}); Placeholder.IsText matches; code: the marker becomes a comment. Document fix: in implementation 12 P2-02, "a signed decimal group otherwise" first exempts those six; applied 2026-09-14 | `src/Ncx.Config/Templates/Placeholder.cs:51` |
| 45 | P2-02a | How does a padded placeholder match T101 for T0101, and what does a wider number render? | D153 | `src/Ncx.Config/Templates/TemplatePattern.cs:55` |
| 46 | P1-03a | How many decimals does a coordinate the VM computes keep? | D121 | `src/Ncx.Core/Geometry/Vec3.cs:42` |
| 47 | P1-03a | Is start = end of a CENTER arc compared within the tolerance; what is a radius 0? | D122 | `src/Ncx.Core/Geometry/ArcResolver.cs:39` |
| 48 | P1-03a | Cylinder plane on a machine whose cylinder turns about an axis other than Z | D146 | `src/Ncx.Core/Geometry/Plane.cs:37` |
| 49 | P2-01 | Which keys must a machine file and a job manifest contain? | D138 | `src/Ncx.Config/MachineConfigLoader.cs:60` |
| 50 | P2-01 | Is the "kind" of VM 3.8 rule 2 the resource type or what the default serves? | answered by language 4.5 (SPINDLE row), 4.10 (TOOL is "the only spindle on a mill") and VM 3.8 rule 2: default_spindle serves work and tool spindles alike; main matches; code: the marker becomes a comment citing them; applied 2026-09-14 | `src/Ncx.Config/MachineConfigLoader.Resources.cs:192` |
| 51 | P2-01 | Should A and B also resolve to the workpiece holder's C axis? | D147 | `src/Ncx.Core/Machine/MachineConfig.cs:293` |
| 52 | P2-01 | Which resource of the default machine gets the role TOOL, holder or spindle? | D128 | `src/Ncx.Config/DefaultMachine.cs:19` |
| 53 | P2-01 | Which spindle do bare SPINDLE and RPM target on the default machine? | D129 | `src/Ncx.Config/DefaultMachine.cs:22` |
| 54 | P2-01 | Which machine-config key holds the arc tolerance of D36? | D139 | `src/Ncx.Config/DefaultMachine.cs:24` |
| 55 | P2-01 | Address of the Doosan Puma 2600SY sub spindle C axis | D179 | `docs/spec/examples/machines/doosan-puma-2600sy.toml:122` |
| 56 | P0-04 | Does a ';' inside braces start the comment? | implementation: a ';' inside braces is an ERROR under either reading, and only the diagnostics of that invalid line differ (no output, state or plugin surface); the marker becomes a comment citing P0-04 and the prose of architecture 4.1 ("honouring quoted strings and {...} expressions"); applied 2026-09-14 | `src/Ncx.Core/Parsing/Lexer.cs:118` |
| 57 | P0-04 | Is ARG with REPEAT an ERROR? | answered by language 4.9 (ARG row: the argument of the CALL in the same block), VM 3.6 and VM 5 (ARG without partner); code: `s_partners` gets ARG = ["CALL"], the marker goes, and a test shows ARG with REPEAT alone is PAR016. Document fix: the P0-04 Parser bullet of 10-phase-0 reads "ARG with CALL, TIMES with CALL or REPEAT"; applied 2026-09-14 | `src/Ncx.Core/Parsing/BlockRules.cs:38` |
| 58 | P0-04 | Should NAME, NUMBER, CHANNEL and the cycle words be reported outside their partner's block? | D116 | `src/Ncx.Core/Parsing/BlockRules.cs:29` |
| 59 | P0-04 | The legal 4.7.1 program CYCLE=RECT_POCKET LENGTH=60 WIDTH=40 does not parse | D114 | `src/Ncx.Core/Catalog/WordCatalog.cs:111`; pointer in `src/Ncx.Core/Parsing/Parser.cs` CheckWords |
| 60 | P0-04 | Which axis words do SHIFT, TILT, TILT_AXIS and SETPOS take (TILT X=1, SHIFT IX=5)? | D117 | `src/Ncx.Core/Parsing/BlockRules.cs:98` |
| 61 | P2-02b | On which line is an unparsable template reported? | answered by phases.md row 2 and implementation 12 P2-01 ("a bad file reports the line") and code-guidelines 6; code: the loader parses each template text on its key's line (ConfigTable.LineOf), instead of TemplateSet reporting it on line 1; a text written by several keys is reported on the first | `src/Ncx.Config/Templates/TemplateSet.cs:14` |
| 62 | P2-02b | What does FindFunctionByCode return for states that are words of their own (ORIENT, RPM, SPINDLE_SYNC)? | D154 | `src/Ncx.Config/Templates/TemplateSet.cs:137` |
| 63 | P2-02b | Which state does FindFunctionByCode answer for a code that several states write? | D155 | `src/Ncx.Config/Templates/TemplateSet.cs:96` |
| 64 | P2-02b | Does a Heidenhain file without decimal_separator write the comma? | answered by heidenhain.md 1 ("the writer must produce it"), 8 rule 2, differences.md (Numbers) and the machine-config 2 comment; code: TemplateSet.DecimalSeparator takes its default from the controller (comma for Heidenhain, point for Fanuc and Siemens), and the NumberFormatter of P3-03 uses the same default; a written key still wins; applied 2026-09-14 (the public TemplateSet.DecimalSeparator) | `src/Ncx.Config/Templates/TemplateSet.cs:114` |
| 65 | P2-03b | Does a later [[cycle]] replace the whole entry or only the keys it writes? | D140 | `src/Ncx.Core/Machine/CycleEntry.cs:194` |
| 66 | P2-03b | Where does the built-in drilling family live? | implementation: Ncx.Core.Machine, for the reason of D107, and VM 3.3 needs the built-in names; the marker becomes a comment citing D107 and VM 3.3; applied 2026-09-14 | `src/Ncx.Core/Machine/DrillingFamily.cs:3` |
| 67 | P2-03b | No key for the kind of a catalog parameter | D115 | `src/Ncx.Core/Machine/CycleEntry.cs:3` |
| 68 | P2-03b | Severities of the checks of a catalog entry | D141 | `src/Ncx.Config/Cycles/DiagnosticCodes.Cycles.cs:7` |
| 69 | P2-03b | How is n of `CYCLE:<controller>=n` written for Fanuc and Siemens? | D144 | `src/Ncx.Core/Machine/CycleCatalog.cs:111` |
| 70 | P2-03b | Default of a Siemens fixed parameter left empty (VARI of CYCLE83) | D181 | `src/Ncx.Core/Machine/CycleCatalog.cs:61`; `tests/Ncx.Core.Tests/Machine/CycleCatalogTests.cs:62` |
| 71 | P2-03b | No key for the words a reader or compiler rule carries | D157 | `src/Ncx.Core/Machine/CycleEntry.cs:74` |
| 72 | P2-03b | Part one's data questions repeated: Q211, Q256 or Q213; Fanuc P on G81; constant PECK on CYCLE83 | D163 (Heidenhain 200 and 203), D177 (Fanuc P), D181 (CYCLE83) | `src/Ncx.Core/Machine/DrillingFamily.Heidenhain.cs:27`, `DrillingFamily.Fanuc.cs:39`, `DrillingFamily.Siemens.cs:113` |
| 73 | P1-01 | VM 2.1 takes the start workplane and origin "from TOML", machine-config has no key | answered by D34 (configuration defaults apply to source readers only); main matches (XY and 0); code: the marker becomes a comment citing D34. Document fix: VM 2.1 reads "XY" and "0"; the marker applied 2026-09-14, the VM 2.1 wording stays for the maintainer | `src/Ncx.Core/VirtualMachine/State/FrameState.cs:18` |
| 74 | P1-01 | VM 2.5 takes function[name] "from TOML", machine-config 5 gives no start state | answered by D34; main matches (no state until FUNC sets one); code: the marker becomes a comment citing D34. Document fix: VM 2.5 reads "none for each [func] entry of the machine"; the marker applied 2026-09-14, the VM 2.5 wording stays for the maintainer | `src/Ncx.Core/VirtualMachine/State/ChannelState.cs:67` |
| 75 | P1-01 | Do a callee's V1..V33 start unassigned or as a copy of the caller's? | D148 | `src/Ncx.Core/VirtualMachine/State/VariableStore.cs:169` |
| 76 | P0-06 | What is a column of language 5 rule 7 beyond ASCII? | D166 | `src/Ncx.Core/Writing/NcxWriter.cs:106` |
| 77 | P0-06 | Trivia placed by line number cannot stay in place where a reader's block lines decrease | D142 | `src/Ncx.Core/Writing/NcxBuilder.cs:190` |
| 78 | P0-06 | Is NcxBuilder.Raw a whole RAW block or a word of the open block? | answered by language 4.1 (RAW has scope block), language 5 rule 6 (RAW ranks among the words of a block), controller-mapping 2 (DC(), ACP(), ACN() read as `C=` plus RAW) and architecture 7 (a whole RAW block is `ReaderBase.EmitRaw`); main matches; code: the marker becomes a comment citing them. Document fix: in the class diagram of architecture 7, Begin and Raw return NcxBuilder; applied 2026-09-14 | `src/Ncx.Core/Writing/NcxBuilder.cs:111` |
| 79 | P0-06 | How is a usage error rendered, which has no file? | D110 | `src/Ncx.Cli/Program.cs:46` |
| 80 | P0-06 | May a byte order mark begin an NCX file, and does format keep it? | D111 | `src/Ncx.Cli/Commands/FormatCommand.cs:89` |
| 81 | P4-01a | What does an index on a variable that is not a SYS_ name mean ($Q1[2])? | D145 | `src/Ncx.Core/Expressions/Evaluator.cs:93` |
| 82 | P4-01a | What does an index that is not a whole number select ($SYS_WEAR_Z[99.5])? | D145 | `src/Ncx.Core/Expressions/Evaluator.cs:177` |
| 83 | P4-01a | Do AND and OR skip their right operand when the left one decides? | D168 | `src/Ncx.Core/Expressions/Evaluator.cs:232` |
| 84 | P4-01a | Do == and != compare strings? | D169 | `src/Ncx.Core/Expressions/Evaluator.cs:234` |
| 85 | P4-01a | What is FRAC of a negative number? | D119 | `src/Ncx.Core/Expressions/Evaluator.Functions.cs:68` |
| 86 | P4-01a | Argument order, range and value at (0, 0) of ATAN2 | D119 | `src/Ncx.Core/Expressions/Evaluator.Functions.cs:199` |
| 87 | P4-01a | How many arguments does each function take? | D118 | `src/Ncx.Core/Expressions/Evaluator.Functions.cs:87` |
| 88 | P4-01a | The unassigned setting lives in the store and in the evaluator; which is the single source? | implementation: one setting, [variables] unassigned (machine-config 7, VM 3.6, D38), carried by VmOptions.Unassigned; P1-02 fills it and builds the store from it, and P4-01 part two passes the same value to Evaluator.Evaluate. Which of the two applies it changes no output and no plugin surface (VariableStore is internal). The plain TODO in ReadUnassigned becomes a comment, or one branch goes. Document fix: the Evaluate signature of P4-01 in 14-phase-4 follows the code; applied 2026-09-14 | `src/Ncx.Core/Expressions/Evaluator.cs:117` (plain TODO) |
| 89 | P4-01a | How does a vars file name an indexed SYS_ register (SYS_WEAR_Z[99])? | D120 | `src/Ncx.Core/Expressions/Evaluator.cs:163` (plain TODO) |
| 90 | P1-02 | Does a switch list of skip_blocks also skip a bare SKIP? | D133 | `src/Ncx.Core/VirtualMachine/SkipBlocks.cs:59` |
| 91 | P1-02 | Which machine-config key holds the arc tolerance of D36? | D139 | `src/Ncx.Core/VirtualMachine/VmOptions.cs:52` |
| 92 | P1-02 | Type, id and axis name of a resource D103 creates on the spot | D170 | `src/Ncx.Core/VirtualMachine/ResourceResolver.cs:58`, `:415` |
| 93 | P1-02 | Is a coolant channel the machine file does not name an ERROR? | D130 | `src/Ncx.Core/VirtualMachine/ResourceResolver.cs:205` |
| 94 | P1-02 | Which verbs does the C= check of VM 3.8 rule 4 cover? | D131 | `src/Ncx.Core/VirtualMachine/ResourceResolver.cs:333` |
| 95 | P1-02 | SHIFT=RESET removes "the shifts", VM 2.1 cuts at the last entry of the kind: which? | answered by VM 2.1, the chain paragraph of language 4.2 ("a RESET of a kind removes that entry and everything after it"), D31 and P1-02; main matches; code: the marker becomes a comment. Document fix: the SHIFT=RESET row of language 4.2 and the SHIFT description in `src/Ncx.Core/Catalog/FrameWords.cs` (rank 90 of `generated/word-catalog.md`) say the last shift and what follows it; the catalog description applied 2026-09-14, the marker in FrameRules.cs is left to the residual review RR-P1-03 and the language 4.2 row to the maintainer | `src/Ncx.Core/VirtualMachine/FrameRules.cs:53` |
| 96 | P1-02 | What does ORIGIN do to the position? | D123 | `src/Ncx.Core/VirtualMachine/FrameRules.cs:71` |
| 97 | P1-02 | Is a different preload kept after TOOL=k? | D132 | `src/Ncx.Core/VirtualMachine/ToolChangeRules.cs:50` |
| 98 | P1-02 | Is SAFE halved under AXIS=X with DIAMETER=ON? | answered by D60, VM 3.3 ("the X values of an AXIS=X cycle are halved like every other X"), language 4.2 and the opening of 4.7 (SAFE is an absolute coordinate along the drilling axis); main matches; code: the marker becomes a comment citing D60 and VM 3.3. Document fix: the AXIS row of language 4.7 adds SAFE; the marker applied 2026-09-14, the language 4.7 row stays for the maintainer | `src/Ncx.Core/VirtualMachine/DiameterRules.cs:37` |
| 99 | P1-02 | Does "PROGRAM=END sets OFF" of VM 4 also set CSS OFF? | answered by VM 4 (the spindle row: "sets OFF" applies to the items that have an OFF value, the direction and CSS), language 4.11 and VM 2.4; main matches; code: the marker becomes a comment. Document fix (optional): the row reads "PROGRAM=END sets direction and css OFF, mode SPINDLE, sync OFF"; the marker applied 2026-09-14, the VM 4 wording stays for the maintainer | `src/Ncx.Core/VirtualMachine/ProgramEndRules.cs:31` |
| 100 | P1-02 | How is an expression value in SHIFT, TILT, TILT_AXIS or ROTATE kept in STATIC mode? | answered by VM 1 (STATIC: "a state variable set from an expression becomes UNKNOWN"; the chain is a state variable, VM 2.1); main does not match; code: TransformEntry holds a value as UNKNOWN (nullable value, or a key in ChannelState.Unknown as SETPOS:C has), and a RESET or ORIGIN that removes an unknown shift makes its axes unknown outside the MACHINE frame instead of folding back 0 (reproducer: HOME X, SHIFT X={$Q1}, SETPOS X=100, SHIFT=RESET leaves X known at 100); the architecture 5 diagram follows | `src/Ncx.Core/VirtualMachine/VirtualMachine.cs:377`, `:417`; `Handlers/FrameHandlers.cs:117`; `FrameRules.cs` CutAt, Fold; `State/TransformEntry.cs` |
| 101 | P1-02 | Is MIRROR=OFF the reset form of the mirror? | D124 | `src/Ncx.Core/VirtualMachine/Handlers/FrameHandlers.cs:126` |
| 102 | P1-02 | On which spindle do syncPartner and syncPhase stand? | D171 | `src/Ncx.Core/VirtualMachine/Handlers/SpindleHandlers.cs:89` |
| 103 | P1-02 | Do SHIFT, TILT, TILT_AXIS and SETPOS take incremental axis words? | D117 | `src/Ncx.Core/VirtualMachine/VirtualMachine.cs:339` |
| 104 | P1-02 | Is the X of a SHIFT a diameter under DIAMETER=ON? | D125 | `src/Ncx.Core/VirtualMachine/VirtualMachine.cs:363` |
| 105 | P1-02 | In which frame are the rotary positions known after TILT_AXIS with MOVE=TURN or MOVE? | D126 | `src/Ncx.Core/VirtualMachine/VirtualMachine.cs:454` |
| 106 | P1-02 | Does "directly after a HOME" (D101) allow blocks in between? | D127 | `src/Ncx.Core/VirtualMachine/VirtualMachine.cs:524` |
| 107 | P1-02 | Which state does a later program of the file start from in STATIC mode? | D134 | `src/Ncx.Core/VirtualMachine/VirtualMachine.Static.cs:51` |
| 108 | P1-02 | D99 entry state when the file's first program has no verb | D135 | `src/Ncx.Core/VirtualMachine/VirtualMachine.Static.cs:102` |
| 109 | P1-02 | Where does an ARG with a name outside V1..V33 go? | D149 | `src/Ncx.Core/VirtualMachine/VirtualMachine.Static.cs:251` |
| 110 | P1-02 | How many passes does a CALL get when TIMES is an expression in STATIC mode? | D136 | `src/Ncx.Core/VirtualMachine/VirtualMachine.Static.cs:368` |
| 111 | P1-02 | At ORIGIN, what happens to an axis known in the workpiece frame of the datum, not through SETPOS? | D123 (with 96) | `src/Ncx.Core/VirtualMachine/FrameRules.cs:71` |
