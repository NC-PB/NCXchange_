# Example tests

The examples of the specification as acceptance tests: `ExampleFormatTests` formats every `.ncx` example of `docs/spec/examples` and expects it back byte for byte (phase 0, P0-06). `check` over the examples (phase 1) and the round trips through the readers and compilers (phase 3) join them here.
