# Fixtures

The inputs of the acceptance tests that are no example of the specification, embedded by `../Ncx.Acceptance.csproj` under their path from the repository root and read through `../Jobs/JobFixture.cs` (`../../README.md`, Fixtures): `nakamura-wy250l.ncxjob.toml`, the job manifest of the Nakamura WY-250L pair, whose two channels run the files `ncx convert` makes of `docs/spec/examples/sources/NAKAMURA_WY250L_O1000.path1.nc` and `.path2.nc` (implementation 16, P6-01; machine-config 8).

Never here: a copy of an example of `docs/spec/examples` (the specification folder stays the single source), an expected output (`../Expected/`).
