# Fixtures of Ncx.Config.Tests

`expected-machine-warnings.txt`: the WARNINGs the eight machine files still report, each with a comment that names the gap in its file; `ExampleMachinesTests.MachineFiles_Warnings_AreTheCommittedList` compares what the files report against it, and a fixed file removes its lines (P2-01, P2-04). The file is embedded by its name in `../Ncx.Config.Tests.csproj`; this README is not embedded.
