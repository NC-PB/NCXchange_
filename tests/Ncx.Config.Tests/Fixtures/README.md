# Fixtures of Ncx.Config.Tests

`expected-machine-warnings.txt`: the WARNINGs the eight machine files report, each with a comment that names the gap in its file; `ExampleMachinesTests.MachineFiles_Warnings_AreTheCommittedList` compares what the files report against it, and a fixed file removes its lines (P2-01, P2-04). Since P2-04 every file loads clean and only the comments remain; a gap that only an open question can close would stand here with its wave-1 question number. The file is embedded by its name in `../Ncx.Config.Tests.csproj`; this README is not embedded.
