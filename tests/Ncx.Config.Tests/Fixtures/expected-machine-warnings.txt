# The diagnostics of the eight machine files: the five examples of machine-config 11 (D104) and the three mills of
# machines/ (P2-04), as ExampleMachinesTests loads them (P2-01). Every line is a WARNING and names a gap in a file;
# a gap that only an open question can close stays here with its wave-1 question number.

# P2-04 fixed the last gaps: the clamps of the spindle tables of mori-ntx1000-mapps.toml and dmg-ctx-840d.toml, and
# the C_SYNC of the Mori Seiki's [spindle_sync], are named functions in [func] now, as in nakamura-ntjx.toml and
# doosan-puma-2600sy.toml (machine-config 5). Every file loads clean, so only these comments remain.
