# Cycle catalog tests

A catalog file loaded entry by entry over the built-in drilling family, every mistake with its line (`CycleCatalogLoaderTests`), the `[[cycle]]` entries of a machine file over its catalog (`MachineCycleEntriesTests`), and the three shipped catalogs of `cycles/` (`ShippedCatalogsTests`: `G81`, `CYCL DEF 200` and `CYCLE81` map the same NCX words); machine-config 6. `CycleDiagnosticCodesTests` keeps the codes inside `CFG150` to `CFG199`.
